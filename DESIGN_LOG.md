# Design Log

A categorized log of design **decisions** and **changes** — grouped by page → section → component so it's easy to find why something looks the way it does. Newest decisions win. For the deeper "why it works" taste analysis and Simon's references/anti-patterns, see `DESIGN_KNOWLEDGE.md §6`. For token architecture see `DESIGN_SYSTEM.md`.

> Format per entry: **what** — *why*.

---

## 0. Global / Design system (`src/index.css`)
- **Monochrome accent · Figma case-study template · no placeholder metrics (2026-09-03)** — a batch off Simon's updated Figma file (`Simon test¨`) plus screenshot feedback. (1) **The orange brand is retired.** The `--brand-*` ramp is gone from the tokens and `--accent` is now simply the highest-contrast ink for its ground — `neutral-0` on dark, `neutral-1000` on light (with `--accent-contrast` inverted to match, and `--accent-on-media` → plain white). Everything routed through `--accent` followed: buttons, links, focus rings, the scroll-progress bar (now white on dark / black on light), the case-study labels. The green availability dot and the feedback colours are untouched — status keeps its own hues. The Design-system page's brand-ramp specimen, `THEME_VALUES` and manifesto line were rewritten to match ("One neutral field; contrast does the accenting."). (2) **Placeholder impact numbers removed everywhere** — the `+6% monthly store revenue` chip is out of the project data and out of `ProjectCard`/the home stack card (component level), and every `content.json` `outcomes` array is emptied, so the Impact band and its TOC entry no longer render on any case page. (3) **The company wordmark chip is off the project cards** (the home stack cards) — the project page hero keeps its logo. (4) **Project pages rebuilt to the Figma template** (node `61:805`): 1256px content column, 160/128 rhythm, title 32/36 + 16/20 lead in a 3-of-7 measure, full-width hero at 1256/700, a `Client · Timeline · Industry · Role` details row (14/18, 48px apart), then `Problem` / `Solution` as quiet 16/20 labels over primary-text copy — the sticky meta rail, uppercase accent kickers, numbered process steps and impact band are all gone. Process notes now land between the imagery (18/24) two images at a time, and the page closes on a hairline + a `Projects` grid of the new cards. (5) **`ProjectCard` is the Figma card 1:1** — the image *is* the card (1256/700, 4px radius, tonal plate behind), discipline chips laid over its top-left corner (`--transparent-dark-24` + 10px blur, pill, 12/16 white), title 14/18 primary and description 14/18 secondary beneath. No card surface, padding or shadow. (6) **Archive** (node `73:2531`) is the same 1256 column: `Archive` at 24/26 medium, 24px down to a 2-column card grid, 48px gutters. (7) **Nav bar aligns to the page content** — new `--content-max: 1256px` token; the header wrapper is `content-max + 16px` and its gutter is `page-gutter − 8px`, so the bar's *inner* edge lands exactly on the content edge (bar 1272 / content 1256, as drawn). (8) **Contact** holds a 1024px-tall frame with the form centred in it (64px top/bottom padding on phones). (9) **Front page rebuilt to the Figma template** (node `61:804`): a left-aligned hero — statement type at 32/36 in a 513-of-1256 measure, a 14/18 lead beneath it, then `View projects` (Button `primary sm`) + `Contact` (Button `secondary sm`) 8px apart — then `Projects` at 24/26 over **full-width project cards stacked 128px apart**. The scroll-driven sticky card stack, its scale/dim rAF loop and the `Selected projects` display heading are retired, and `ProjectGrid` is gone: the project list now lives in `src/data/projects.js` and every page maps `ProjectCard` itself. Hero copy is Simon's own — the existing headline plus the About-page intro verbatim (the Figma's "5 years of experience" hero copy is his to swap in if he prefers it). (10) **Companies + Testimonials restyled to match** (neither is designed in Figma yet): both now open with the same 24/26 medium section heading (`Companies`, left-aligned with everything else — the uppercase mono "Worked at" overline and the centred row are gone), and testimonials are **flat quote blocks** — wordmark, quote at 16/20 primary, name at 14/18 primary over role at 14/18 secondary, in a 3-up grid 48px apart. No surface, no shadow, no serif quote glyph, no initials avatar. The company hover cards keep their glass but their spec rows now use the same 14/18 label/value pairing as the case pages. (11) **`--content-max` → 1440px** (Simon's call, up from the design frame's 1256) and **the nav bar spans exactly that column** — the header wrapper is `--content-max` with the page gutter, so the bar's edges line up with the content on every page (its own 8px padding insets the brand/nav inside that), and the bar's top inset is now the design's 20px. (12) **Dead code removed** — `TagRow`, `OutcomeGrid`, `OutcomeStat`, `ProcessStep` and the `ProjectGrid` component all went with the templates that used them. (13) **Front-page follow-ups (same day, screenshot-driven):** the hero sits on a **1024px content box with the 128px padding on top of it** (1280 total) with its content centred; the **Companies row lost its heading and is centred**, and on phones it becomes a **single-row marquee** — the set rendered twice, translated `-50% - gap/2` over 24s linear so it loops seamlessly, with a `mask-image` fade on both edges (tooltips hidden on touch, `animation: none` + a swipeable row under reduced motion); **testimonials went back onto a surface** — `--surface-color-secondary`, 24px padding, 8px radius, and **every line at 14/18**. (14) **The page shell owns the vertical rhythm; the header floats (2026-09-03, late):** the routed page wrapper in `App.jsx` is now a flex column (`.pageShell`) with **128px between every section** (64 on phones) and the same **128px before the footer**, so pages no longer carry `margin-bottom: 164`/section paddings of their own — Home, About, Playground, StyleGuide, Archive, Contact, ProjectPage and MotionLab all had their vertical spacing stripped and handed to the shell (they keep only their horizontal gutters and internal spacing). The **header is `position: fixed`** rather than sticky-in-flow, and the shell reserves its space with a new `--header-clearance` token (`--header-inset 20 + --header-bar 60 + 80px of air`), so **every page starts exactly 80px below the bar's bottom edge**. Verified across all nine routes: clearance 80, gaps 128 (64 mobile), 128 before the footer, zero horizontal overflow, and the glass bar still frosts the content scrolling under it. The **front-page hero is a fixed 724px band** with its content centred. (15) **Hero opening + card hover label (2026-09-03, last):** the **companies row moved inside the hero section** — the hero is a flex column (`gap: 128`) holding the copy and the marks, and it's **content-sized** — the fixed 724px band was tried and dropped, because a fixed height forces the band's slack either above the headline (bottom-anchored) or below the marks (centred, which made the second gap 295). With no band, **every gap on the front page is the same 128**: bar → copy, copy → companies, companies → projects (64 on phones). `--header-clearance` moved from *bar bottom + 80* to *bar bottom + **128*** so the air above a page's first content matches the gaps between its sections — every page now opens on the same rhythm. `WorkedAt` dropped its own page gutter since it's nested in a padded section now. Also added a **cursor-following "View project" label** to every project card: it trails the pointer by 16px with an eased rAF glide (`0.16`/frame), clamped 12px inside the frame so it never clips at a corner, using the same on-media chrome as the discipline chips (`--transparent-dark-40` + 10px blur, `--text-on-media`, pill radius, 12/16). **The system cursor stays visible** — the other common take on this effect hides it (`cursor: none`), which costs more in orientation than it buys; the label is `aria-hidden` (the card is already a link), snaps instead of gliding under `prefers-reduced-motion`, and is `display: none` on touch. **The pointer is tracked in VIEWPORT coordinates and the element-relative target is recomputed every frame while hovering** — pointer events only fire on movement, so scrolling (which moves the card under a stationary cursor) left the label glued to the content instead of the cursor; the rAF loop now stays alive for the duration of the hover and re-reads the frame's box each frame, which also covers resizes and layout shifts. (Browsers re-evaluate `:hover` after a scroll, so the fade in/out stays correct on its own.)

**Every leading is an explicit even pixel · focus gets its own hue · the Figma file cleaned out (2026-09-03, last):** (1) **No ratio line-heights left anywhere** — all 42 of them were converted to explicit px on a 2px grid, computed from each rule's own font size (`type-label` 1.2 → 18px, the control roles → 14px/12px, the header rows 1.25/1.375 → 16/18/20/22, and so on). The two fluid display sizes on the Design-system cover clamp their leading in px too, so both ends land on the grid. The unused `--leading-*` ratio tokens went with them. (2) **Focus is an accessibility affordance, so it keeps a hue**: a new `--focus-ring` token (blue-600 light / blue-400 dark — 5.17:1 and 7.79:1 against their grounds, well past the 3:1 WCAG 2.2 focus requirement) now drives every focus treatment; they had all been the monochrome accent, which is nearly invisible against black or white ink. Figma's `Focus/ring` follows the same two stops — it was **still aliasing Brand/600 / Brand/500**, the last orange reference in the file. (3) **Deleted from Figma what nothing referenced** (verified against all 2,826 nodes, every variable alias and every style binding first): the whole `Brand/*` ramp (11), `On media/Accent`, the retired `Font size/2xs · 4xl · 5xl`, and the `type-display`, `type-display-sm`, `type-title`, `type-subtitle`, `type-micro` styles. The file's size steps are now exactly the seven the code uses. (4) **The project-page template was re-synced after Simon's edits**: `type-body` is 16/**24** and `type-body-sm` 14/**20** in the file now, and the template moved Problem/Solution *and* the process notes to `type-body-sm` — so the 18px block variant is gone from the page and its `size` prop with it. (5) The **hero fills its frame again** (`cover`): fitting it inside the frame left letterbox bands, which read as a mistake.

**Control type roles + the Figma file brought in line (2026-09-03):** (1) Buttons, chips, badges and nav labels now use two dedicated roles — **`type-ui-14`** and **`type-ui-12`**, same sizes as the body roles but at **100% leading** so the control's height and padding own the box. Padding was compensated where the leading was carrying height, so the drawn geometry is unchanged: the project-card chip is still 24px (6 + 12 + 6), the cursor label still 28px, buttons still 32/40/48. (2) **The Figma file was updated to match the code** (via the Plugin API): `Accent/default` → `Neutral/1000` light / `Neutral/0` dark, `Accent/contrast` inverted to match, `Accent/soft` → black @12% / white @16%, `Accent/on media` → `On media/Text` — **no orange resolves anywhere in the file any more** (verified on the Button component's accent variants). The light text greys followed the AA work too: `Text/secondary` → `Neutral/600`, and a new **`Neutral/550` (#6b6b6b)** primitive now backs `Text/tertiary` in light (it was pointing at `Neutral/200` — `#e5e5e5`, 1.2:1 on white). Dark mode was already correct and is untouched. The four Regular text styles and the two new `type-ui-*` styles could **not** be written from here: BDO Grotesk isn't installed in the remote Figma environment, and font mutations require loading it.

**Logo-grid gap → 4px phones / 8px up (2026-09-04):** 16px between ~98px tiles read as more air than the marks themselves. Now **4px below 768, 8px above** — less air on the small screen, more once there's room, rather than one value scaled down. Tightening also hands width back to the cells (117 → **125px** at 430). Verified: 430 → 3 cols / 4px / 125px, 768 → 4 cols / 8px / 166px, 1440 → 5 cols / 8px / 250px, no overflow at any width.

**Year field, bottom-aligned card copy, gallery hover label (2026-09-04):** (1) **`Timeline` → `Year`, and the schema follows.** The project details row said "Timeline" over values like `2024 · ~3 months`; it now reads **Year** over a bare year. Every project already carried *two* fields holding the same thing — `when` (bare year) and `timeline` (the decorated version) — so they're collapsed into a single **`year`** across all eleven `content.json` files, the loader, and the defaults, with `when` retired (it was consumed in exactly one fallback). Two projects lost trailing detail in the process: `apple-home-app` dropped "self-directed concept" and `zliide-app` dropped "~3 months" — flagged to Simon, since the first is arguably worth keeping somewhere. (2) **Skill-card copy is bottom-aligned.** The 4-line reserve left short copy floating one empty line above the card's padding edge. The copy now sits at the BOTTOM of its reserve, so the last line lands on the padding edge — measured **24px text-bottom gap on every card at 1280 / 1440 / 1920, with title spread still 0**. This is a deliberate trade Simon chose: titles aligned *and* text bottom-flush is only geometrically possible if the slack sits between title and copy, so that gap now varies by a line where the copy is short. `-webkit-box-pack: end` was tried first and measured as a **no-op**, hence the flex wrapper plus an inner span carrying the clamp. The one 4-line outlier at 1440 (`User Research & Testing`) was trimmed so the row is uniform at the common widths. (3) **The cursor-following label is now shared.** `useCursorLabel` extracts the placement maths (offset, edge clamp, sticky flip with hysteresis, eased placement) out of `ProjectCard` so the picture gallery can use the same thing reading **"View details"**. Two traps in the wiring, both caught before shipping: `<Reveal>` spreads `{...props}` **last**, so a `ref` passed to it silently clobbers its own IntersectionObserver ref and the tile would never reveal; and the label can't live inside `.zoom`, which scales on hover and would scale the label with it. Hence a separate `.hotspot` wrapper that is the positioning context and the hover area. (4) **About bio** opens its second paragraph "I work end to end with product design".

**Hero was 74px right of every other section — a flexbox shrink-to-fit (2026-09-04):** `.hero` is a **flex column** while `.section` is a **block**, and both wrap their content in the same `.container` (`max-width: --content-max` + `auto` side margins). As a *flex item*, those `auto` margins make the container shrink-to-fit its content instead of filling the line, so it resolved to **1291px instead of the 1440 cap** — and centring that narrower box pushed its left edge in by exactly 74px. The headline, lead and CTAs were therefore indented relative to `Projects`, the project cards and the header bar. Fixed by giving `.container` **`width: 100%`** so it fills first and *then* caps, which makes the flex and block cases resolve identically. Verified at 1920 / 1440 / 430 / 375: every container on Home shares one left edge and one width (233/1440 at 1920), the header bar lands on the same edge, and About / Archive / ProjectPage were already correct (their containers aren't flex children) — Home was the only page affected.

**Skill set revised; description box sized to the narrowest column (2026-09-04):** (1) **The set changed, not just the copy.** `UX Research` and `User Testing` were the same competency split in two — testing is a research *method*, not a peer discipline — which read as padding five skills out to eight; **merged into `User Research & Testing`**. **`UX Writing` dropped**: nothing in the role history evidences it, and the standard is that every skill listed resurfaces in a case study. Two added from the record the old set never named: **`AI Product Design`** (in Simon's own LinkedIn headline, and the Lena AI assistant work) and **`Data-Heavy Interfaces`** (a thread through three employers — Zliide's brand dashboard and internal dashboard, the Lenus food diary, Adservice's self-service interface). *Caveat: LinkedIn's skills endpoints were down — `/details/skills/` renders an empty list and the skill-associations overlay returns "Something went wrong on our end" — so this is derived from role descriptions and the profile headline, not the literal skills list.* (2) **The 3-line description reserve was wrong and was clipping copy.** The skills column is 271px at 1440 but only **227px at 1280**, and copy that sits on three lines at the wider measure needs four at the narrower — so on a 1280 laptop the clamp cut every card. The reserve and clamp are now **four lines, sized to the narrowest common column**: nothing truncates at 1280/1440/1920, titles stay on one line, and title-top spread is **0** at all three. The clamp stays as a guardrail — `min-height` reserves a floor but doesn't cap, so without it a single card running long drags its title out of the row (a scrollbar narrowing the column by 15px was enough to trigger exactly that). (3) **"Frontend Development" now fits one line on phones** — the 1-up card gives it a 295–350px measure instead of 134px, so the label needed no shortening after all.

**Mobile pass — skills, testimonials, experience table, hero (2026-09-04):** four fixes off Simon's live-phone screenshots, all phone-only. (1) **Skill cards go 1-up and short.** At 2-up the copy was squeezed to a ~24-character measure *and* each card still stood 260px tall, so barely two fit a screen. Full width buys the measure back (~45 chars, so the copy runs to two lines instead of five) and lets the card be **200px** — measured **4.4 per screen** at 430x932. 168px was tried first and overshot to 5.2, which flattened the photo into a strip behind the text. Description drops to 14/20 with its own tracking, and its reserve becomes two lines so the titles still align. (2) **Testimonial cards: 24 → 16px side padding on phones**, vertical untouched — the quote is the content and 24px a side was eating measure a 375px screen can't spare. (3) **Experience table: the job moves to the right column.** ~~Date + role across the top, company + industry beneath~~ — that was a misread of the ask and was corrected the same day. The left column is the **date alone**; the right column is the **job, company first**: `Lenus` over `Product Designer` over `Health & fitness software`. The date is the index you scan down, the company is what you're actually looking for, so it leads its own column instead of being paired off with a date. **Placement only (`grid-area`), DOM order untouched**, so screen-reader order and the desktop 4-column table are unchanged. (4) **Hero headline 32 → 24 on phones** (with 28px leading and `--tracking-24-tight` — both are calibrated per size): 32 ran to **five lines** on a 430px screen and pushed the lead and CTAs most of a screen down. Now three lines, with the CTAs at 420px — visible on the first screen.

**Company logo grid → 3-up on phones (2026-09-04):** the 2-up step shipped the day before was legible but turned a 21-logo wall into eleven rows of scrolling on a phone. Now **3 / 4 / 5** across phone / tablet / desktop, with the cell frame tightened to 8px padding and a 56px floor so the mark keeps the width it needs at a ~98px cell (375px viewport) — verified 3 columns with no overflow at both 375 and 430, marks rendering 101×101 in square 117px cells, nothing cropped. (5-up, the original, rendered each mark ~20px wide and unreadable; 2 and 3 are both legible, so the tiebreak was scroll length.)

**Motion polish · mobile grids · Playground retired · LinkedIn-grounded copy (2026-09-03, late):** a long batch off Simon's rapid-fire screenshot feedback.

(1) **Motion audited against the transitions.dev token rules.** The site keeps its OWN `--dur-*`/`--ease-*` scale (calmer than the generic one), so the audit used the *rules* — open/close asymmetry, hover in/out, stagger totals, delay hygiene — not a competing token set. **One real bug:** the header dropdown transitioned `opacity` but not `visibility`, and `visibility` is discrete — it flipped to `hidden` the instant `.panelClosed` applied, so the 220ms close fade never rendered. Fixed with a `visibility 0s linear <dur>` lane plus the asymmetry (**open 200ms → close 150ms**, no `left`/`width` glide on the way out). The lightbox had the same symmetry problem — one `0.28s` for open *and* exit; **close is now 150ms** via a per-variant transition, and its open scale moved `0.98 → 0.96` to match `--motion-scale-in`. Five ad-hoc values now reference the tokens (ThemeToggle thumb + segments, the 404 CTA, Footer links/lift, IntroLoader, the View-Transitions theme cross-fade) — two of them were Tailwind leftovers (`cubic-bezier(0.4, 0, 0.2, 1)` and a `border-color, text-decoration-color, fill, stroke` property list). Dead `.fx-stagger > *` rule removed (`Stagger.jsx` stopped injecting `--i` when it moved to Motion springs). **Left alone, flagged to Simon:** the dropdown row stagger (80ms x up to 12 = ~1.6s to the last row, and the footer at 520ms currently lands *mid-list*, not after it) and the Worked-at tooltip stagger (~1s) — both far past the ~300ms guideline but both deliberate, so they're his call.

(2) **Project-card hover zoom removed** (`.zoom` -> `.media`, `scale(1.04)` gone). The card's hover affordance is now the cursor-following "View project" label alone. Applies everywhere `ProjectCard` renders (Home, Archive, next-project, DS page) — it's one component. The **label's corner flip now glides**: the flip decision was applied instantly to the final x/y while only the *cursor* was eased, so crossing a threshold teleported the label by its own width. The ease now runs on the label's **placement** (target computed from the raw pointer, position eased toward it), with `FLIP_HYSTERESIS` 24px so it can't chatter on the line.

(3) **Mobile grids.** Skills stay **2-up** on phones (the <=640 one-column override is gone) and the card steps down a rung for it — `aspect-ratio: auto` + `min-height: 260px`, 12px padding, title 20->16, body 16->12. The **logo grid's `.cols5` was fixed at five columns at every width** (marks rendered ~20px wide on a phone); now **2 / 3 / 4 / 5** across 0 / 480 / 768 / 1024 with tighter cell padding under 768. The **picture gallery's `auto-fit` floor of 240px allowed exactly one tile** on a 375px screen, so a 9/16 portrait filled the whole viewport; a 96px floor under 640 lands it **3-up**. The **front-page marquee** gets a roomier `--marquee-gap` (32 -> 48) and a faster `--marquee-speed` (24s -> 20s); the keyframe already derives its travel from the gap, so the loop stays seamless.

(4) **Playground retired** — page, CSS, route, lazy import, title-map entry and footer link, plus `components/shader/` (`ShaderBackground` + `shader.js`), which the page was the only consumer of. `--font-mono` now has no call sites at all.

(5) **Copy re-grounded in Simon's LinkedIn.** The hero and About bio were adjective soup ("passionate about human psychology", "beautiful and user friendly designs") — nothing checkable, and nothing a hiring manager could act on. Both now lead with specifics: the hero names the role and the product ("Product designer at Lenus, designing the world's leading health coaching platform") over a lead line dense with scope (end-to-end process, three domains, design systems, ships production code). The About bio names real work — the coach onboarding portal taken to global rollout, the in-platform AI assistant, the medical programme designed under compliance constraints. **Freelance dates corrected** (Jan 2023 - Aug 2024 -> **Jan 2024 - Jul 2024**, per LinkedIn).

(6) **Skill cards: titles aligned, copy rebuilt around business value.** The block is bottom-anchored in the frame, so a 2-line description pushed its title DOWN while a 3-line one held it up and the title row went ragged. `.description` now reserves the longest description's height (`min-height: calc(3 * 24px)`; 5 x 18 on phones) so the titles land on one line — **measured: title-top spread 0 across both rows, cards uniform at 438px, nothing clipped.** The reserved text sits **top-aligned, not bottom**: the title-to-description gap is what binds them as a unit, so it's the one measurement that must never vary, and slack falls at the bottom where there's no shared baseline to break (the card's lower edge is image, not type). Bottom-aligning would hold that edge steady at the cost of a gap that changes per card — much louder. **Copy rewritten twice**: first cut from 5-6 lines of benefits filler to one plain sentence, then re-pointed at business value at Simon's request — what he does AND what it's worth, all eight landing on exactly three lines. The trap avoided both times is the original register: it *did* chase value, but by stacking three abstract benefits per card ("...ultimately leading to increased user retention and loyalty"), which proves nothing. One concrete consequence beats three generic ones. Remaining deviation: **"Frontend Development" cannot fit one line at the phone 2-up** (171px at 16, 148px at 14 against a 134px measure; 12px would match the body copy and kill the hierarchy) — left at two lines pending Simon's call on shortening the label.

(6-) **Section rhythm 160 -> 200, one section-heading role, 12px heading gap.** The shell's rhythm was already **160**, not the 128 CLAUDE.md claimed (docs had drifted); it's now `--space-200` for the gap, the pre-footer padding and `--header-clearance` alike. **Every section heading is one role**: 16/24 medium with **12px** to its content — About's `.label` was 16/24 at 32px gap, Home's `.sectionHeading` and Archive's `.title` were 24/26 at 24px, so Projects/Testimonials/Archive stepped down to match. The DS page was advertising a scale that **didn't include 160 at all** (it listed 128 / 164 / 200) and labelled 128 "between sections" — both corrected, and `--space-164` is now marked unused because nothing references it.

(6d) **Copy, third pass — researched rather than guessed.** Simon rejected two earlier attempts, so the register came from the actual hiring-side standard: portfolio headlines work as *role + years + domain* with the value in the supporting line, and capability copy should **name methods and tools** rather than assert outcomes, because a named method is checkable in an interview and a claim about value is not. Hero is now "Product designer with five years of experience across health tech, retail and adtech"; the eight skill cards name flows/IA/journey mapping, Figma components, tokens, coded prototypes, interviews, moderated + unmoderated testing, interface copy, and React/CSS. The About bio is **employer-agnostic** — an About page outlives any one job, and leading with the current role dates it the moment he moves; the current role lives on the Experience table right below.

(6e) **Multi-device audit before shipping.** Swept /, /about, /archive, /contact, /project/* and /design-system at **375 / 768 / 1024 / 1440 / 1920**, light and dark: zero horizontal overflow anywhere, no missing alt text, no empty links, one h1 per page, no console errors. Grid steps verified (skills 2/2/4/4, logos 2/3/4/5, gallery 3-up on phones). **One real bug found and fixed:** the new carousel dots measured **8x24** — under the 24x24 floor in WCAG 2.5.8 — because `.dot` carried vertical-only padding. Widening the button would have pushed the dots apart, so they got an invisible 44x44 `::after` overlay instead, the same trick ThemeToggle uses for its 28px segments. (One false alarm worth recording: an audit probe reported `h1: 0` on Archive and Contact, which was the probe sampling during the lazy route's Suspense fallback, not a missing heading.)

(6a) **Contact page: the 1024px frame is gone.** `.section` carried `min-height: 1024px` + `align-content: center` from the Figma frame, but the page shell already supplies the vertical air (`--header-clearance` above, 128px before the footer) — so the frame stacked a second, larger void on top of it and pushed the headline most of a screen down. Now content-driven at every size: **section 1024 -> 578px, headline top 268px, the whole form clearing a 1440x900 laptop in one screen (form bottom 846 of 900)**; phones unchanged in behaviour at 594px with the gutter as the only padding. Deliberately NOT switched to a viewport height despite the "max 1vh on laptops" phrasing: `100vh` is precisely what breaks on phones (the address bar resizes it mid-scroll — which is why the ask itself carved mobile out), and a true `max-height` would clip a form that must never clip. The content is already short enough to fit, so bounding it buys nothing and only adds a failure mode. Consistent with CLAUDE.md rule 6.

(6b) **Hero headline generalised.** Naming the employer read as a LinkedIn headline and dated the page to one job; the headline is now about Simon's profile ("Product designer taking work from user research all the way to shipped code") and the legitimacy signal — five years, the three domains, Lenus and the platform's scale — moved into the lead beneath it, where it's still the first thing read but no longer the identity.

(6c) ~~**Skill-card copy cut to 2-3 lines.**~~ All eight descriptions ran the same shape — "do X, which does Y, ultimately leading to Z" — 5-6 lines of benefits copy that buried the skill itself and read as templated filler (the register CLAUDE.md warns about). Rewritten to one plain sentence each. Measured at 1440: **every title on 1 line, every description on 2-3** (271px measure). At the mobile 2-up the same copy reflows to 4-5 lines on a 134px measure, which is the same text at half the width rather than a fault; the one real deviation is **"Frontend Development", which cannot fit one line there at any size on the scale** (171px at 16, 148px at 14, and 12px would match the body copy and kill the hierarchy) — left as two lines pending Simon's call on shortening the label.

(7) **Testimonials are a carousel on phones** (`TestimonialCarousel`, <768px, rendered as an either/or via the new `useMediaQuery` hook so the quotes appear once in the DOM). Swiping is **native scroll-snap**, not a JS drag — real momentum, trackpad and keyboard for free, and it can't desync from the indicator. The indicator is the **segmented-pill pattern** (Apple / Vercel / Linear): inactive slides are 8px dots, the active one widens to a 32px pill whose interior sweeps over the 9s dwell, so the row carries position *and* timing. Auto-advance **stops permanently on first interaction** (WCAG 2.2.2), idles off-screen and while the tab is hidden, and never runs under reduced motion. *Note: this crosses the "no carousels/auto-advance" testimonial anti-pattern in DESIGN_KNOWLEDGE §6.2 — Simon asked for it explicitly, scoped to mobile.*

**Scale cut to seven sizes · greys to AA · dead code swept (2026-09-03):** (1) **Type scale → 12 · 14 · 16 · 18 · 20 · 24 · 32**, which is exactly what the Figma pages use (12/14/16 body + UI, 18 for the case notes, 20 for skill-card titles, 24 for section headings, 32 for page titles). The 10px, 40px and 48px steps went, and with them `type-display`, `type-display-sm` and `type-micro`; `type-title` (20) and `type-subtitle` (18) were duplicates of `body-xl`/`body-lg` and were folded into them. Fourteen call sites moved: the Contact/Playground/Motion-lab headlines and the 404 now use `type-heading` (32), the cookie dialog and playground sub-heads `type-body-xl`. Figma has no 28px step, so the headings are 24 and 32. (2) **Light `--text-color-tertiary` → `neutral-550` (#6b6b6b)**: at `neutral-500` it measured **4.35:1 on the tonal card surface — under AA** (it only passed on the white page ground at 4.74). 550 clears both (5.33 page / 4.89 card). Secondary stays `#525252`, which is already AAA on both (7.81 / 7.17). AAA for tertiary isn't reachable while keeping a third distinct grey — it would need to be ~#595959, i.e. secondary. Dark mode is AAA throughout except secondary on a card (6.90, AA). (3) **Thirteen unused components deleted** (`PrinciplesList`, `RecognitionStrip`, `ExperienceTimeline`, `HeroBackground`, `AvailabilityPill`, `FeaturedQuote`, `ShowreelTile`, `StatBand`, `Modal`, `Section`, `Tooltip`, `BodyText`, `Heading`) — `Tooltip` went dead with `TagRow`; the rest predate this work. The last ESLint warning is gone too: the lightbox's key handler now owns its stepping logic, so the effect depends only on primitives (no `useCallback`, which the React Compiler rejects). (4) **The project-page hero is `object-fit: contain`** — it meets whichever frame edge it reaches first and is never cropped, with the tonal plate showing as the letterbox. Content plates keep their natural ratio.

**Medium (500) is the weight floor (2026-09-03):** *Simon: "we want to use the font weight 500 at a minimum; so no one using 400."* This also settles the one genuine ambiguity in the Figma variables — `type-body-lg` and `type-body-sm` carry the style name *Regular* but bind their weight field to `Font weight/medium`, and the intent is 500. So `body` now defaults to Medium and the four roles that were Regular (`type-body-xl`, `type-body-lg`, `type-body-sm`, `type-caption`) moved to it, along with every local `--weight-regular` in the header, project card, skill card, project page and featured quote. Audited in-browser across all pages: **nothing renders below 500** except the Design-system page's own "Ag" Regular specimen, which exists to show the weight. The `--weight-regular` token stays for a deliberate exception.

**Section rhythm → 160 · the leading reset removed · skill-card imagery (2026-09-03, last):** (1) the page shell's rhythm moved **128 → 160** (80 on phones) and `--header-clearance` followed it, so every page still opens on exactly the gap it uses between sections; the front-page hero's copy ↔ companies gap tracks it too. Page-internal spacing the Figma draws (the case-study 128s, the footer's 128) is unchanged. (2) **The universal reset carried `line-height: 100%`**, which pinned every element's leading to its own font size — so type set on a container (the About experience table, and the pattern the Figma files use everywhere) never reached its children: the table rendered 14/14 instead of 14/18. Removing that one declaration lets the design system's leading inherit as intended. (3) `type-display` synced to the Figma role: **48/52** at desktop (was 1.1) with its −5% tracking; the Design-system page's type table was rewritten with the real synced values (it had been quoting stale ratios and trackings for nine of its fourteen rows) and now marks the code-only roles as such. (4) The **four skill-card backgrounds** were pulled from the Figma frame, re-exported to 900px WebP (1–5 KB each) into `src/assets/skills/`, and cycled across the eight cards in the grid's own alternating order; a bottom scrim (`transparent-dark-8 → 40 → 60`) sits under the copy so the white 20/24 title and `#d4d4d4` body stay legible over the lighter gradients, in place of the per-card washes the mock applies to half of them. Note these gradients are warm — the only colour on the site outside project imagery. (5) The **companies row got its tonal tile back** (`--surface-color-secondary`, 8px radius, 24px padding, 5-up): the marks looked tiny because every source PNG is a square canvas with the wordmark floating inside it, so they scale by width (160px) rather than height.

**About page rebuilt to the Figma frame `61:803` (2026-09-03):** five sections on the shell's 128 rhythm — (1) **Intro** on a 10-column grid 24px apart: the `About` label on columns 1–2, the portrait on 3–5 (360/343, 4px radius, cover) and the bio on 7–10 at 16/20 Medium secondary, with the CV button under it; (2) **Experience** on a 7-column grid — the label on 1–2, a four-column `Time · Company · Role · Industry` table on 4–7 at 14/18 regular secondary, rows 32px apart, filled with Simon's own roles and dates (Lenus · Beefit · Zliide · Freelance · Adservice) and the industries from the companies data; (3) **Skills** — 4-up cards at 305/430 with 12px gaps, copy bottom-aligned, on the **inverted surface** (`--surface-color-contrast-primary`) with the design's exact ink pair (title 20/24 regular `text-contrast-primary`, body 16/20 medium `text-contrast-tertiary` = `#d4d4d4`); the Figma card carries a photo behind that type, so the inverted surface stands in until there are real images; (4) **Companies I've designed for** (not in Figma) restyled to match — the marks now sit flat on the page ground at the front page's 24px optical size, no tiles, no shadows; (5) **Pictures** — a 5-column gallery, 8px gaps. Every section opens with the same quiet 16/20 Medium label. **Dropped** (they aren't in the design): the full-viewport portrait hero with floating glass chips, the mission-statement panel, and the About copy of the testimonials — the testimonials live on the front page now, so the footer and nav links point at `/#testimonials`. Two pieces of the frame were left out on purpose: the full-width 1256/700 photo band and the skill-card photography, both placeholder imagery in the file.

**Type roles re-synced to the Figma variables (2026-09-03):** the file is the source of truth for the roles it defines, so the `.type-*` roles now carry its exact px line-heights instead of ratios — `type-heading` 32/**36**, `type-heading-sm` 24/**26**, `type-body-lg` 18/**24**, `type-body` 16/**20** and now **Medium** (it was Regular/1.5), `type-body-sm` 14/**18**, `type-caption` 12/**16** — plus a new `type-body-xl` (20/24 Regular) that the Figma About frame uses. Tracking already matched exactly (−4% → `tracking-tight`, −3% → `snug`, −2.5% → `base`), as did every font size, both weights (400/500), the whole surface/glass/transparent set, `Feedback/success`, the footer fades, and the spacing + radius scales. Two variables were left deliberately out of sync, both flagged to Simon: Figma's `Accent/default` is still the retired orange `#db5320`, and its `Text/secondary` is `#a3a3a3` even on the light frames (2.3:1 on white — under AA for body copy, and the DS page promises AA), so light secondary stays `#525252`. `.type-display*`, `type-title`, `type-subtitle` and `type-overline` have no Figma counterpart yet and were left as they are.

**Scroll-reveal trigger moved earlier (2026-09-03):** `useReveal` observed with `threshold 0.1` + `rootMargin 0 0 -28% 0`, i.e. an element had to cross **72% of the viewport height** *and* have a tenth of itself inside the box — on a 745px project card that meant it was a third of the way up the screen before it moved. Now `threshold 0` + `-10%`: anything reveals as soon as any part of it crosses **90% of the viewport height**. Also: the project-page **opening gap dropped 64 → 24** (the Figma frame changed), and the cursor label's offset tightened to **10px right / 8px above** the hotspot.

**Footer → matched to the Figma component 1:1 (`25:170`):** `padding: 128 / gutter / 24`, top corners 8px, the content column aligned to `--content-max`, 4 equal columns 32px apart, **128px between the columns and the © row** (which keeps its own 24px top padding), headings at 14/1.2 medium primary 16px above their list, links at 14/**18** regular secondary with 8px vertical padding and a 2px list gap, © line at 12/16. The radial lift from `--surface-color-secondary` at top-centre and the fading hairline were already an exact match and were left alone, as were the site's link hover states (not drawn in a static mock). Phones halve the 128s to 64. (16) **Mobile QA pass** — walked all nine routes at 375px: zero horizontal overflow anywhere, mobile menu / dropdowns / theme toggle / cookie banner / project-card taps / footer links / 404 CTAs / contact form / About lightbox all exercised. Three real fixes: the **About lightbox now locks page scroll** while open (the gallery scrolled under the open image), the **hero CTAs get a 44px min-height on phones** (the design's `sm` 32px is a desktop size), and the **28px theme-toggle segments get an invisible 44px hit area** under `pointer: coarse`. (15) **Eight case images re-exported from their pre-compression originals** — a 2000px *long-edge* cap had crushed the tall full-page screenshots to 463–1267px wide (the Adservice one rendered at 578px in a 1256px frame, visibly soft); they're now capped on WIDTH at 1800px.
- **Chips → theme-split `--chip-surface` (visible in both modes) + layered shadows gain a subtle top edge (2026-08-26)** — two screenshot-driven tweaks. (1) **Tag chips (`Badge .solid`) now use a new `--chip-surface` token** that sits **one tonal step OFF the card in each theme**: light = `--surface-color-secondary` (Simon: "the one just after the complete white one" — one step down from the white card, ≈ `#f5f5f5`); dark = `--surface-color-tertiary` (≈ `#2d2d2d`, one step **lighter** than the secondary card ≈ `#1a1a1a`). *(First tried plain `secondary` for both — but in dark the card IS `secondary`, so chips vanished; Simon: "make the badges on dark a surface color lighter than the bg." The theme-split token fixes dark while keeping light exactly as approved.)* Verified: dark chip `rgb(45,45,45)` on card `rgb(26,26,26)`, light chip `rgb(245,245,245)` on white. (2) **The `--shadow-sm/md/lg/xl` layered stacks now lead with one subtle UPWARD (negative-Y) layer** so a card's **top edge is defined too** — a white card on the white page was losing its top (bg and card tones too close). The top layer reads *clearly* (`0 -1px 3px` α.05 on `sm` up to `0 -2px 7px` α.065 on `xl`) while the bottom/side penumbra was **lengthened** so the fade is gradual and natural, not a tight cut-off: `sm`'s far layer grew `5px/10px`→**`10px/20px`** blur (and `md`–`xl` proportionally, up to `52px/92px` on `xl`) with the outer alphas *tapered* (.03→.026→.02) so it's a soft longer tail, not a heavier shadow. The **near-bottom contact layer is the strongest** (α.06 on the tight `0 1px 2px` step) so the card reads as *grounded/resting* on the page, the mid layers carry the side falloff, and the top sits a hair lighter (α.045) — natural hierarchy **bottom > sides > top** (light from above), an enclosing shadow, not a box. *(Iterated with Simon: top first pass too faint → bumped ~2.5×; then sides/bottom felt abrupt → lengthened the falloff; then the long taper left the bottom under-grounded / weaker than the crisp top (backwards) → restored a stronger tight contact layer at the bottom + eased the top.)* Mirrored in the dark tokens for parity (dark cards still use `--surface-card-shadow: none` and lean on tonal contrast, so it's moot there). Verified both themes in-browser: `sm` computes with the `rgba(17,17,17,0.02) 0 -1px 2px` top layer, chips `rgb(245,245,245)` in light, zero console errors. Synced `DESIGN_SYSTEM.md` (chip + elevation notes).
- **Layered elevation as TOKENS + full token pass on the testimonial card + quote leading fix (2026-08-24)** — (1) **The `--shadow-sm/md/lg/xl` elevation scale is now LAYERED stacks** (Ahlin/Comeau: one light source, geometric offset+blur growth, decreasing per-layer alpha) instead of single blurs — `xs` stays a hairline. So the layered-shadow "theory" is a first-class token now, not an inline value; `--surface-card-shadow` references `var(--shadow-sm)` (light) / `none` (dark). (2) **Testimonial card fully tokenised** (Simon: "tokens for everything, don't hardcode"): quote uses the **`type-body`** role, name + role use **`type-label`**, avatar sizing/type use `--space-40` / `--text-xs` / `--weight-medium` / `--tracking-wide`, logo row `--space-32` — no raw px/rem/weights left. (3) **Quote leading fix** — the Propane-matched `1.3` was cramped on the long multi-paragraph quotes; `type-body`'s **1.5** leading restores comfortable body rhythm. Verified both themes, no console errors.
- **Cards → white surface + layered shadow; testimonial card matched to Propane; popover/wipe cohesion (2026-08-24)** — (1) **Content-card surface tokens** (`--surface-card-bg` / `--surface-card-shadow`, theme-aware): light = **surface-primary (pure white)** + a **clean multi-layer "stacked" shadow** (one downward light source, offset+blur growing 0.5→2→5→12, α .035→.02 — Ahlin/Comeau method → soft realistic penumbra, subtle); dark = tonal **secondary surface, no shadow** (black-on-black + dark shadow is invisible). Applied to testimonial/skill/project/impact/logo cards + project-page **breakout images** (dropped the heavy `--shadow-xl`). (2) **Testimonial card matched 1:1 to usepropane.ai/platform** (inspected live): card **padding 24 / gap 24**, **logo 32px**, **quote 16px/1.3 medium secondary**, **name + role both 14px/20 medium** (name primary, role secondary), and a **placeholder avatar** — a 40px round neutral circle with the recommender's **initials** (no fabricated photo). (3) **"Worked at" popover** matched to the header dropdown exactly (`--glass-bg` + 20px blur + no border + `0 4px 16px` shadow). (4) **Page-transition wipe → `--surface-color-secondary`** (theme-aware `#f5f5f5`/`#1a1a1a`) so it matches the card surface (subtle, cohesive). Verified both themes, zero console errors.
- **De-glassing pass: flat tonal cards, glass reserved for chrome, subtle shadows, matched header/dropdown glass (2026-08-18)** — a long iterative pass (Simon, screenshot-driven) pulling the "glassy" look off content and reserving it for floating chrome. (1) **Content cards → flat solid tonal surfaces:** `TestimonialCard`, `SkillCard`, `ProjectCard`, `OutcomeGrid` (impact band) and `LogoGrid` tiles dropped `glass-panel`/`glass` for a solid **`--surface-color-secondary`** fill — **no border, no shadow, no backdrop-filter, no hover spotlight**. (Tried a footer-style top-lit gradient on the cards in dark, then reverted to flat per Simon.) (2) **Tag chips (`Badge .solid`) → flat `--surface-color-tertiary`** (one tonal step off the card), no glass fill/sheen/border. (3) **Glass de-glassed:** removed the **outer hairline border** from `.glass`/`.glass-panel` (kept `--glass-border` for internal `border-glass` dividers) and the **inset "lit top-edge" highlight** from `--glass-shadow`/`-lg` (it read as a white line on panel tops) — glass is now tint + blur + soft sheen + a plain drop-shadow. (4) **Shadows shrunk:** `--glass-shadow` `0 8px 32px`→`0 2px 8px`, `--glass-shadow-lg` `0 20px 60px`→**`0 4px 16px`** (fixed the oversized "Worked at" popover + dropdown shadows in one go). (5) **Header ⇄ dropdown glass matched:** `.panelCard` now uses the header bar's exact `--glass-bg` tint + **20px** blur (was the more-opaque `--glass-bg-strong` + 16px), so bar + menu read as one surface. Navbar blur set to **20px** on `.bar`. (6) **Light glass** = **white @ 60%** (`--glass-bg`, chrome only); dark ≈ `#1c1c1e`. (7) **Concentric radii:** project-card image → `--radius-sm` (4px) inside the card's `xl` (8px) — inner < outer. (8) **Testimonial quote glyph → greyscale** (`--text-color-tertiary`, opacity 0.4), not the warm accent. (9) **Button hover** strengthened so the bg change is clearly visible on every variant (primary 20% shift, accent 16%, outline/glass/ghost → `--glass-item-active`). Verified in both themes, zero console errors. *(Net effect: colour lives only on the accent; content is flat tonal surfaces; glass is a quiet chrome material.)*
- **Sharp corners (radius tier → 0) · light glass darkened · navbar 20px blur · footer border toned down (2026-08-17)** — (1) **All cards & pictures square** (Simon: "0px border radius across all components"). The 8px radius tier **`--radius-md`/`lg`/`xl`/`2xl`/`3xl` → `0`** in `index.css` — one change squares every card, panel, and image (the Media component + all image frames reference these tokens). `sm` (4px micro) and `pill` (avatars, chips, nav bar) kept. NB this also squares **buttons & inputs** (they use `md`) — accepted as part of the sharp direction; revert by giving them their own non-zero token if wanted. **↩ Reverted same day** — Simon asked to restore the rounding, so the whole tier is back to **8px** (the site keeps its `≤8px` tight-radius convention). (2) **Light-mode glass was too light** (near-white on white → invisible). Retinted from `rgba(255,255,255,.46/.56)` to a light **grey** `rgba(222,222,226,.8)` / `rgba(214,214,218,.85)` (≈ #e3e3e5 / #dcdcde) — a visible frosted panel, still airy. Dark glass unchanged. (3) **Navbar blur → 20px** (Simon) — overridden on `.bar` (was `.glass`'s 12px `--glass-blur-md`); other `.glass` users unaffected. (4) **Footer top border was too white** — the reference's white hairline actually carries `opacity:0.15` (inspected live), so the effective centre is white@~15%, not pure white. Remapped from `--text-color-primary` (opaque) to **`--glass-border`** (white 12% dark / black 8% light) — subtle, theme-aware. Verified both themes: cards/images/buttons square, avatars+chips+nav still round, light header a visible grey, navbar 20px frost, footer hairline subtle, zero console errors.
- **Dark glass darkened · footer matched 1:1 to marckuiper · page-wipe z-index fix (2026-08-17)** — (1) **Dark-mode glass too bright** (Simon) — the old `--glass-bg` `rgba(58,58,62,.58)` / `--glass-bg-strong` `rgba(48,48,52,.66)` were lifted to frost the old *warm mesh*; on the flat near-black ground they composited to a bright grey. Darkened to **`rgba(40,40,44,.6)`** (chrome ≈ #1c1c1e) / **`rgba(32,32,36,.68)`** (panels/cards ≈ #19191c) — still translucent, sheen+hairline carry the frost. Affects header + all glass-panel surfaces. (2) **Footer gradient matched 1:1** to marckuiper.com/services (inspected live), using our **semantic tokens** so it auto-themes: bg `radial-gradient(50% 50% at 50% 0%, var(--surface-color-secondary), var(--surface-color-primary))` (their `#121212→#000` → our dark `#1a1a1a→#000`; light `#f5f5f5→#fff`); top border `::before` 1px `linear-gradient(90deg, transparent, var(--text-color-primary) 50%, transparent)` (their pure-white hairline → our text-primary: white in dark / black in light). Content untouched. (3) **Page-transition wipe z-index bug** — the `.wipe` (z-index 1500) lived inside `<main>` (a z-index:1 stacking context, needed so the mesh sits at -1), so the later-in-DOM `Footer` (z-index:1) painted **over** it during a switch (visible on → /contact). Fixed by **`createPortal`-ing the wipe to `document.body`**, so its 1500 competes at the root context (the originally-intended "above header, below intro loader"). Verified: wipe is now a `<body>` child, fixed, z-1500; header + footer + all glass darker; footer gradient exact; zero console errors.
- **Flat-direction refinements: nav pill removed · card shadows + hover retired · wipe recoloured · container 1440 · hero cap · 2px scrollbar (2026-08-17)** — a batch extending the flat B&W direction. (1) **Nav triggers** — removed the sliding glass `navPill` entirely (plus its `hovered`/`pillStyle` state, the positioning `useLayoutEffect`, and `navRef`/`activeRouteKey`); triggers are now **pure text** that shifts secondary→primary on hover/active (added a `color` transition), dropdowns behave exactly as before. (2) **Content cards flattened** — `box-shadow: none` on `TestimonialCard`, `SkillCard`, `ProjectCard`, `OutcomeGrid` (impact band), overriding `glass-panel`'s `--glass-shadow-lg` **at component level** (an unlayered CSS-module `.card` rule beats the `@layer utilities` glass rule automatically — same trick as ProjectCard's existing `backdrop-filter:none`). Floating chrome (nav bar, dropdown panel, tooltip, modal, mobile menu) **keeps** its shadow — it needs the elevation. (3) **Hover retired** on Testimonial + Skill cards — dropped the `group`/`fx-spotlight` cursor-glow (Project cards keep their image `scale(1.04)` zoom). (4) **Page-transition wipe** → `var(--accent)` (was `--surface-color-primary`, which went invisible white-on-white / black-on-black on the flat grounds) — a bold on-brand orange sweep, clearly readable in both themes. (5) **`--container-max` 1920 → 1440px.** (6) **Hero** min-height capped: `min(calc(100vh - header), 1024px)` so it doesn't oversize on tall screens (content just centres within the cap). (7) **Archive dropdown scrollbar** → **2px**, bled flush to the card's right edge (`margin-right: -space-12` + matching `padding-right`). Verified in-browser: pill gone + dropdown intact + text-only colour states, all four card types flat, 2px flush scrollbar, zero console errors. **Follow-up (same day):** (a) **wipe** recoloured accent → **neutral `--neutral-500` grey** (the orange was too loud; grey reads on both grounds); (b) **cursor-spotlight `.fx-spotlight` retired site-wide** (`display:none`) — it was painting an orange hover bloom on cards in light mode; (c) **Archive scrollbar** thumb → `--glass-item-hover` tint (lighter than the default border); (d) **Footer** given a marckuiper-style **gradient treatment** — a transparent base with a soft top-centre radial glow (`--footer-glow`) + a centred gradient hairline **top border** (`::before`, `--footer-edge`) that fades to transparent at the corners; neutral tints (not the reference's purple), content untouched. Footer glow is intentionally subtle (~6%/4.5%) — Simon to confirm intensity in his browser.
- **Background → clean black & white; retired the warm/orange atmospheric wash (2026-08-17)** — *Simon: "Remove the weird orange-ish background … just have a white and black background for the different settings. Less is more."* The site's warm "ember" mesh — a core "depth-from-light / colour-as-light" device — was neutralised to a restrained achromatic field in all three sources: (1) **`.meshBackdrop`** (App, the primary backdrop glass frosts) → dark base `#0a0a0a` + faint white lifts & a shadow-pool corner; light base `#ffffff` + the faintest neutral shade — enough texture for glass to still frost, zero hue. (2) **Hero WebGL shader `PALETTE`** (HeroBackground, demoted layer) → neutral near-black / near-white drift (was `#d8602b`/`#7c3618` ember). (3) **`.shader-bg-fallback`** (no-WebGL) → flat neutral surface (dropped the `--accent-soft` radials). **Kept the orange accent** on buttons/links/active-nav — against the now-neutral field it reads *stronger*, sharpening the "restrained neutral + one warm accent" DNA rather than fighting it. This is a deliberate softening of the old "atmospheric depth from warm shaders" direction toward a simpler editorial black/white canvas (see DESIGN_KNOWLEDGE §6.8). *Left in place:* the StyleGuide `.blurStage` (a deliberately-busy demo surface, not the site bg). **Follow-up (same day):** Simon asked to go *fully flat* — removed the achromatic mesh blobs **and** the shader drift entirely (both now render a single solid field: `#0a0a0a` dark / `#ffffff` light; `#0a0a0a` chosen over pure `#000` to avoid OLED smear/harsh edges — swap in `App.module.css` if literal pure black is ever wanted), and **neutralised the About `.portraitGlow`** to a soft white bloom `rgba(255,255,255,0.06)` (was the orange `--accent-soft`). Glass now frosts a flat colour, reading via its own translucency/tint (fine — GLASS §8). Verified in-browser both themes: dark = flat near-black, light = flat white, portrait glow achromatic, glass + nav still legible, zero console errors.
- **Theme switch → calm full-page colour cross-fade (retired the circular reveal) (2026-08-17)** — *Simon: "Can we make the transition from dark to light more smooth? … Maybe it just makes more sense to do it instant? Or maybe from top-right corner?"* The old switch was a **1200ms View-Transitions circular clip-path reveal** blooming from the toggle — the single most-copied portfolio theme animation, and slow/showy enough to read as **decorative + attention-grabbing** (against the DNA). Replaced with a **snapshot colour cross-fade**: keep `document.startViewTransition(apply)` but drop the `clipPath` `root.animate` entirely and let the UA's default fade run, tuned on `::view-transition-old/new(root)` to **320ms `cubic-bezier(0.4,0,0.2,1)`** (was `animation: none`). Because it dissolves a full-page *bitmap*, gradients + glass + the hero shader all cross-fade uniformly — reads as "instant, but buttery." Kept: reduced-motion → instant, `theme-anim` per-property fallback (trimmed 1200→400ms) for no-VT browsers. Also **swallow the transition promise rejections** (`vt.ready/finished.catch(()=>{})`) — a rapid re-toggle skips the in-flight transition and would otherwise log an `InvalidStateError` unhandled rejection. Chose cross-fade over instant/corner-reveal with Simon (he picked "smooth cross-fade"). Verified in-browser: light↔dark both directions land correctly, mid-dissolve captured, zero console errors. Synced `DESIGN_SYSTEM.md` motion table + the `/design-system` Theme-toggle `purpose`.
- **Header dropdown width now *animates* between menus + roomier About/Contact (2026-08-17)** — *Simon: "the dropdown width just changes instantly, can we animate smoothly from width to width? and increase the minimum width so About and Contact are a bit larger?"* Root cause: `.panel` was `width: max-content`, and **intrinsic keywords can't be transitioned** — the existing `transition: width` was dead, so every menu switch snapped. Fix = drive the card off an **explicit measured px width**: a `useLayoutEffect` measures the intrinsically-sized `menuContent` (`offsetWidth`, unaffected by the card's `%`-width), adds the card chrome (26px = `space-12`×2 + 1px border ×2), clamps to `[MIN_PANEL_WIDTH 300, min(92vw,560)]`, and writes px onto `.panel` imperatively (React never manages `style.width`, so it survives re-renders and the transition baseline stays the last-painted px → old→new glides at 420ms emphasized, together with the existing `left` glide). Card fills that px via `width:100%` + `overflow-x:hidden` (clips content spill mid-grow; the card's own drop-shadow lives outside the box, unclipped). `menuContent` kept `width:max-content` + **`min-width:274px`** so narrow menus render roomy and their full-width rows fill the card instead of hugging left. Result: About 207→**300px**, Contact 266→**300px**; Projects (368) / Archive (414) unchanged. Verified in-browser at desktop: measured inline widths correct, About/Contact visibly roomier, Archive glides wider, lint clean. *(The width **tween** itself is CSS `transition` on an explicit px — mechanically animatable now; Simon to eyeball the motion feel in his browser since the preview realm can't paint the in-flight frames reliably.)*
- **About testimonials now identical to Home + `text-wrap` polish on project pages (2026-06-10)** — *Simon: "make the testimonials 100% identical to the ones on the frontpage," and "can we use text-wrap balance/pretty on project-page text to make it more balanced/beautiful?"* (1) **Testimonials unified:** the About cards had drifted from Home — **no logos** + each quote as **one wall-of-text string** + Frej's company labelled **"Adservice"** (Home: "Adtraction"). Root fix = **one source of truth**: extracted the canonical data (theme-aware `logoNode` SVGs + 3-paragraph `text` arrays) into **`src/data/testimonials.jsx`**, imported by BOTH Home and About (which now `.map` the same array through the same `TestimonialCard`). About's `.testimonialsGrid` changed to Home's exact `auto-fit minmax(min(340px,100%),1fr)`. They can't drift again. *(Company now reads "Adtraction" on both, matching the front page Simon chose as truth; the quote text still says "at Adservice" verbatim from the letter — flagged to Simon, not fabricated.)* (2) **`text-wrap`:** `balance` on the headings that wrap (`ProjectPage .title`, `ProcessStep .title`) to even line lengths; `pretty` on the body prose (`.description`, `.lede`, `.prose`, `ProcessStep .body`) and the testimonial `.para` (both pages) to kill orphan last-line words. Purely a line-break-distribution change — no sizes/colours/spacing touched. Verified in-browser: About = 3 cards, SVG logos, 3 paragraphs each, "Adtraction", 3-col grid; project page computes balance on titles / pretty on prose; zero console errors.
- **Light-mode `--border-color-primary` softened — was a near-black hairline (2026-06-10)** — *Simon (screenshot of the Apple Home case-study process steps): "these dividers are waaaay too black on white mode."* Root cause was a light-theme token bug, not the component: light **`--border-color-primary` was `var(--neutral-850)` (#1e1e1e)** — correct for dark mode (subtle dark-on-dark) but a near-black 1px line on white, exactly the "hard border" the DNA rejects (depth from light, never boxes). It was the **only** real consumer (`ProcessStep .step` `border-top`; also the DS swatch). Fixed at the source → light **`--border-color-primary: var(--neutral-200)` (#e5e5e5)**, a soft editorial hairline that keeps the semantic relationship intact (primary subtler than secondary `--neutral-300`, mirroring how dark mode orders them). Dark mode untouched (#1e1e1e). Updated the StyleGuide "Border · Primary" swatch (`light: #1E1E1E → #E5E5E5`). Verified in-browser both themes: light dividers now `rgb(229,229,229)`, dark still `rgb(30,30,30)`. *(Below-fold + reveal-gated, so verified via computed styles — Simon to eyeball on his browser after deploy.)*
- **DS page beauty + best-practice pass: manifesto · type ladder · anatomy · accessibility (2026-06-09)** — *researched what makes top-tier DS pages (Geist, Linear, Stripe, Polaris, Material 3, Carbon, Spectrum, Primer, Apple HIG) visually beautiful + canonical, via 3 parallel agents, then implemented the genuinely-missing moves.* (1) **Cover → a voiced manifesto:** pulled the principles out of the prose lead into 4 sharp one-liners with a quiet accent mono index — *"Depth from light — never from borders. · One warm accent on a neutral field. · Type is the hero; whitespace is the frame. · Motion stays calm — it settles, never grabs."* (Kholmatova: few, opinionated, memorable). The lead is now one clean sentence. (2) **Type scale → the canonical specimen *ladder*** (replaced the boxed specimen cards): live glyphs at true size on the left, the spec (`token · size/leading · weight · tracking`) as a quiet right-aligned **mono column with `tabular-nums`** so every numeral locks to one razor-clean edge (verified: all 13 rows' spec right-edge = 1345px @1440); hairline rows, no boxes; largest→smallest so the page demonstrates its own scale. On mobile it stacks and the big samples *wrap* (no right edge to protect). (3) **Anatomy diagrams** for the two flagship components — the most-cited "documented, not just shown" tell, previously absent. A reusable `<Anatomy>`: the component large on a small static glass stage with numbered **accent markers** at each part + an ordered legend (name + role). **Button** (Container · Label · Icon slot · Focus ring) with markers ringing the perimeter so the label stays clear; **Input** (Label · Field · Hint) as a clean left call-out column. (4) **New Accessibility section** (last Foundations entry + TOC link) — 6 commitment cards (Contrast AA · Focus-visible · Reduced-motion · 44px targets · Keyboard · Meaning) + a **keyboard map** table (Buttons/links · Theme toggle · Form inputs · Data display · Lightbox), every row verified against the real components (native buttons/inputs, the segmented `aria-pressed` toggle, the chart hover/focus tooltips, the lightbox's `Escape`/`Arrow` handlers). (5) **Polish:** `tabular-nums` added to `.monoMeta` + swatch step/hex so all spec numbers align. Removed the now-dead `Specimen` component + `.specimen*`/`.typeScale` CSS. Verified light + dark (tokens invert cleanly: accent brand-600→brand-500, cards surface-2→neutral-900) and mobile 375 (zero horizontal overflow; ladder/anatomy/a11y all reflow to one column). **Then, on Simon's go, three more (he picked all three):** (6) **Light ⇄ Dark side-by-side** in the Color section — the same mini-composition (surface · text hierarchy · accent button · chip) rendered from *both* themes' resolved values at once (the dark tokens live on `html.dark`, so a nested live-dark subtree isn't possible — `THEME_VALUES` constants mirror the tokens), so the semantic inversion is shown as a feature. (7) **Contrast pills** — a computed-not-guessed WCAG pill (`relLuminance`→ratio→AA/AAA, rule #8) on the accent specimen, each text-role swatch (top-right overlay) and both theme-preview halves; theme-aware via `useTheme` (verified: text-primary 19.26:1 AAA · secondary 7.17 AAA · tertiary 4.35 AA-Large · accent 3.98 AA-large · dark-half 6.90 AA). (8) **Patterns section** (new TOC group, after the Library) — components *composed*: a glass card carrying the live **cursor spotlight**, and **on-media chrome** (impact pill + title over a project photo using the `--transparent-*`/`--text-on-media`/`--accent-on-media` tokens, no backdrop-filter). IA reordered so scroll = TOC order: Foundations (…motion, **accessibility**) → Library → **Patterns**. *(Motion feel + the spotlight glow + the on-media photo = Simon to eyeball; the preview pauses reveals and lazy images.)*
- **About gallery: larger thumbnails, ~5-up (2026-06-09)** — the picture grid (`ImageGrid`) was `auto-fit minmax(200px)` → 6 columns at 1440–1500px. Bumped the min tile to **240px**, so it now shows **5 columns at 1440–1500** (tiles ~250px, up from ~206px), 4 on smaller laptops (≤~1366), 2–3 on tablets/phones — bolder thumbnails, still fully responsive. Verified 5 @1500/1440, 4 @1340, no overflow.
- **Home stack-card brand logos get an on-media glass chip (2026-06-09)** — the company wordmark (top-left of each stacking project card) was a bare white logo with no backing, so it disappeared over light imagery (e.g. the Zliide dashboard). The card's scrim only darkens the *bottom*, so the top logo had nothing behind it. Wrapped it in an on-media glass chip (`.stackLogo`): a gentle dark wash (`--transparent-dark-40`) + a faint light hairline (`--transparent-light-24`) + 8px radius — legible over light OR dark photos, frosted-glass read, **no `backdrop-filter`** (the card scales on scroll — rule #7). Logo size unchanged (svg still `20px × optical-scale`). Inset moved from `padding` to `top/left` so the padding can be the chip's.
- **Idle route-chunk prefetch — instant navigation (2026-06-09)** — routes are code-split (lazy), so the first visit to each page briefly fetched its JS chunk. `App.jsx` now keeps each route's `import()` as a named function and, once the browser is **idle after first paint** (`requestIdleCallback`, 3s timeout; `setTimeout` fallback; both cancellable), prefetches the likely-next chunks — **Archive · About · Contact · ProjectPage · StyleGuide**. So later navigations resolve instantly from cache (verified: all five are fetched while still sitting on `/`, before any navigation). Trade: a little idle bandwidth up front (~120kB gzipped, non-blocking) for snappy page-to-page nav — exactly the "longer first load, faster after" ask. (Content-hashed chunks also cache across visits via the browser. Rare routes — Playground/MotionLab/NotFound — stay on-demand to keep the warm-up lean.)
- **Badge drops `backdrop-filter` (Archive reveal perf) (2026-06-09)** — giving the Badge the glass material a moment earlier re-added a `backdrop-filter` to it; on the Archive page each scroll-revealing card carries ~3 tag badges, so every badge re-sampled its blur each animation frame = the lag. Removed the badge's `backdrop-filter` entirely — the frosted look is carried by paint only (sheen + noise + translucent tint + hairline), which is what actually reads as glass (the blur was nested-dead inside the card anyway). Result: the revealing Archive cards now have **zero painting `backdrop-filter`** (card + badges + impact pill all paint-only), so the reveal is pure GPU opacity/transform — smooth — with the glass look intact. (Same rule-#7 move already applied to the card surface + LogoGrid.)
- **DS page → full-width layout, ¼ nav · ¾ content, aligned rail (2026-06-09)** — the cover used to centre its content at 72rem *inside* a gutter, while the body centred itself at 72rem *then* padded — so the "On this page" rail sat one gutter right of the cover text, and everything was capped narrow. Now the cover and body share ONE container (`max-width: var(--container-max)` 1920, centred, gutter padding; the cover's inner 72rem cap removed), so the rail aligns flush-left with the cover text (both at the gutter), and the page uses the full available width. The body grid went `200px 1fr` → **`1fr 3fr`** (¼ sidebar · ¾ content). Verified: cover + rail share the same left edge; content = 3× the sidebar.
- **Badge = real frosted glass · circular portrait · hero centered in its section (2026-06-09)** — (1) **Badge finally reads as glass:** `.solid`/`.active` were a flat translucent tint + a (nested-dead) blur, missing the `background-image` sheen + noise that make `.glass` look frosted. Added `--glass-sheen` + `--glass-noise` (with `background-blend-mode: normal, soft-light`), using `background-color` (never the `background` shorthand, which wipes the image layers). Now the DS "Badges & tags" specimens AND the Archive card tags carry the full frosted material regardless of nesting (the look comes from sheen+tint+noise+hairline, not the blur). (2) **About portrait** → full circle (`--radius-pill`) for max roundness, matching the circular header/footer avatars. (3) **Home hero** content centered: the `margin-top: -header` pull-up wasn't taking effect (page-enter wrapper), so content overhung low; switched to `min-height: calc(100vh - var(--header-height))` + symmetric padding, which centers the name/headline/CTAs at the true middle of the section (verified offset 0).
- **DS page visual explanations · hero centered · tooltip fix · bg consistency · avatar photo · 48px page tops (2026-06-09)** — a batch toward the design-system page being a top-tier example, plus several page fixes. (1) **DS foundations now *show*, not just tell:** Motion renders each easing as its actual **cubic-bezier curve** (a small SVG plot beside the animated track — reads even when motion is paused); Radius **demonstrates the 8px clamp** (lg/xl/2xl swatches all resolve to the same 8px corner, labelled "lg → 8" etc.); Spacing steps gained **usage labels** (control padding · card padding · between groups · between sections…); the cover lead is now a confident **philosophy line** (depth from light, never boxes; one warm accent; calm motion). (2) **DS background:** removed the cover-only accent bloom that glowed below the header but not behind it — the page now uses ONLY the global mesh (same as the front page); removed the shader-in-glass demo (kept the `ShaderBackground` component for future no-glass shader sections). (3) **Home hero** content is now vertically **centered** in the viewport (`min-height: 100vh` + `justify-content: center`, not padding-driven top-weighting). (4) **"Worked at" tooltip:** the reveal now covers EVERY row (was capped at 4 via `nth-of-type`), so adding the "Business model" row no longer hid the Website row or left an empty gap. (5) **Avatar photo:** all four of Simon's avatar spots (header brand, header About menu, footer Avatar, About portrait) now load `public/simon.jpg` via a shared `src/lib/avatar.js`, with an `onError` fallback to the memoji so nothing breaks until the photo file is added. (6) **Top spacing** on Contact / About / Archive raised 24 → **48px** below the nav.
- **Same-page anchors no longer trigger the page-switch wipe · About-menu rows → ghost Button (2026-06-09)** — (1) **Anchor-nav rule:** clicking the hero **"View projects"** (`#projects`) replayed the full curtain wipe even though the page didn't change — because the wipe mounted on the *first* navigation after load regardless of whether the **pathname** changed. Fixed in `App.jsx` `AnimatedRoutes`: it now tracks the previous pathname and plays the wipe **only on a real `pathname` change**; hash-only / same-page anchors just smooth-scroll (`ScrollToTop`). Verified: "View projects" → no wipe (hash updates, smooth-scrolls); home → /archive → wipe still plays. Rule added to CLAUDE.md. (2) **About dropdown rows** (About me · Companies · Skills · Testimonials · Pictures) now use the shared **ghost `<Button>`** — so they get the signature **text-roll hover** + the glass hover fill, matching the "Read full story" CTA. Full-width rows (label left, chevron as `iconRight` right) via `.aboutList .aboutItemBtn { justify-content: space-between }`. (Bonus: combined with the anchor rule, jumping between About sections while already on /about now smooth-scrolls with no wipe.)
- **Design-system page → one documented section per component (2026-06-09)** — *researched how top-tier systems (Polaris, Material 3, Atlassian, Carbon, Geist, Stripe, Apple HIG, Twilio Paste) structure component docs, then rebuilt the library to match.* The lumped "Components" tile-grid is gone; each component is now its **own anchored `<section>`** (`ComponentSection` helper) following the universal per-component pattern: **name → one-line present-tense purpose → live staged examples (labeled `Tile` specimens on the glass material, never a hard box) → a few terse, imperative guidelines** (only where they add signal). Sections: Buttons · Badges & tags · Form inputs · Avatar · Theme toggle · Location · Media · Cards · Data display. The **sticky sidebar now lists every component** under the Foundations / Library groups (the standard DS index). Kept deliberately editorial per §6.7 — no props tables / install snippets / code toggles (eng-doc furniture), no numbered eyebrows, generous whitespace, one consistent rhythm (component↔component 64px via the section rhythm; within a component 24px). Verified: 9 sections render, sidebar lists all, no overflow at 1440 or 375. *(Motion/visual feel is Simon's to eyeball — the preview pauses reveals.)*
- **Lightbox nav arrows flank the image (2026-06-09)** — *the gallery prev/next arrows were pinned to the far viewport edges (miles from a centred portrait image); now they sit **16px from the image** each side, inside the image row (`ImageGrid` — the arrows moved into `.row`, which already had a 16px gap; `flex-shrink:0` holds them while the image shrinks to fit). The old "fast tap falls through and closes the lightbox" mobile bug that originally forced the edge-pinning is now independently guarded in JS (backdrop closes only on `e.target === e.currentTarget`; stage + controls stop propagation), so it's safe. Verified 16/16px on desktop and no overflow on mobile.*
- **"Worked at" tooltips gained a Business model row (2026-06-09)** — *added a **Business model** field to the home trust-row tooltips: Lenus / Beefit / Zliide = **B2B2C**, Adtraction = **B2B** (real, supplied by Simon). Sits under Industry, above Period. Field named "Business model" (B2B/B2B2C are business models; reads naturally beside Industry / Period / Company size).*
- **Dropdown footer CTAs → ghost `<Button>` (2026-06-09)** — *the "View all projects / View full archive / Read full story" links in the header dropdowns now use the shared `<Button variant="ghost" size="sm" as={Link}>`* so they inherit the signature **text-roll hover** + the glass hover fill, instead of a one-off plain link with an opacity fade. The trailing arrow stays as `iconRight`; a `.footerCta` class pulls the button flush-left (`margin-left: -12px`) so the label lands exactly where the old link text sat. The bespoke `.footerLink` class was removed (dead). All three menu CTAs converted for consistency. (Ghost = `text-color-secondary` at rest → primary on hover, which is the closest variant to the prior look.)
- **Glass-card reveal perf · white nav pill (light) · footer top radius · Contact=Archive spacing · grouped DS index · audit sweep (2026-06-09)** — *kill the scroll-reveal jank, fix light-mode pill greyness, and tidy several pages.* (1) **Archive/Home card lag fixed:** `ProjectCard` (`.card`) **dropped its `backdrop-filter`** — these cards scroll-reveal (Archive) and scroll-stack with a scale (Home), and re-blurring a large moving surface across many cards at once was the dominant jank source (rule #7 / GLASS §8). The mesh behind is smooth so the blur was invisible anyway; the frosted look stays via the `glass-panel` tint + hairline + noise + sheen. Same treatment applied to the **nav pill** (`.navPill` dropped `backdrop-filter` — nested-in-glass = dead blur + it transform-slides) and the **project hero logo plate** (`.heroLogo` dropped blur — it's inside a `scale-in` Reveal + over a scaling hero, so the frost was dead AND janky; the strong translucent fill carries legibility). (2) **Nav pill, light mode:** was `--transparent-dark-12` (12% black → flat grey); now new theme-aware **`--glass-pill-hover`/`--glass-pill-active`** tokens — LIGHT = white frost (`--transparent-light-60`/`-90`) + the `--glass-sheen` for a lit top edge, so the selected/hover pill reads as glass, not grey. Dark keeps a white-frost lift. (3) **Footer** gets an **8px radius on its top-left/top-right corners** so it reads as a panel rising over the page. (4) **Contact spacing now matches Archive exactly:** dropped Contact's negative-margin pull-up trick for Archive's plain `padding-top: var(--space-24)` — verified both headings now sit at the same offset (h1 top = 150px, header bottom = 84px, 66px gap, not clipped). (5) **DS page index grouped** into **Foundations / Library** with quiet group labels (top-tier DS-nav convention) — clearer, less flat; DS cover now reveals `immediate` (it's the page hero). (6) **Audit fixes:** removed a duplicate `id="main-content"` on the Playground `<section>` (invalid HTML — the global `<main>` owns the landmark); replaced two stale hardcoded `calc(100vh - 80px)` header offsets (About hero, ProjectPage `.notFound`) with `var(--header-height)` (the header is 88px). *(Note: a code-audit flagged "header clips Archive/ProjectPage/Playground content" — verified FALSE in-browser; the flex-column sticky header reserves its space, content sits 66px below it. Not changed.)*
- **Hero entrance = mount cascade, not scroll · slower per-word headline (2026-06-09)** — *the hero is on screen at load, so its entrance must be time-based, not scroll-gated; and the words should settle calmly, one at a time.* **Bug:** the Home hero CTAs and the "Worked at" trust row sat **below the scroll-observer's trigger line** (`useReveal` rootMargin `-28%` → the bottom 28% of the viewport is a dead zone). Measured at scrollY 0: CTA-row top ≈ 563px, WorkedAt top ≈ 731px, trigger line ≈ 518px on a 720px viewport — **both below it**, so they stayed at `opacity: 0` until the user scrolled (which, on the hero, they never do → "buttons never animate in"). **Fix:** new **`immediate` prop on `<Reveal>`** — plays the entrance on **mount** (skips the IntersectionObserver) for above-the-fold first-load content. Applied to the Home hero kicker + CTAs and the WorkedAt row (`revealDelay` prop tails it onto the cascade after the CTAs), and to the About hero cascade (headline → portrait → 6 chips) which had the identical latent bug. Scroll-reveal stays the default for genuinely below-fold content. **Slower headline:** new **`durationMs` prop on `WordReveal`** raises the per-word rise from `--dur-slow` (450ms) → **900ms**, and `stepMs` 70 → **120ms** cadence — words now glide in one-by-one over a relaxed ~2s. Final Home timeline: kicker 150ms → words 500→1460ms (each 900ms) → CTAs 1700ms → WorkedAt 2000ms. *(Preview note: the backgrounded `preview_eval` realm pauses rAF, so Framer's delayed springs don't run there — verify motion feel in a real browser. Home end-state verified structurally: CTAs + WorkedAt reach opacity 1 at scrollY 0 on a 720px viewport.)*
- **Glass logo plate · nav glass hover/active · cinematic hero entrances · Contact top spacing (2026-06-09)** — *more legible logos, consistent glass chrome, a calmer first-load, tighter Contact.* (1) **Project hero logo** now sits in a frosted-glass plate (top-left, over the in-flow hero image — `backdrop-filter` works there); **theme-aware text** (dark on the light plate / white on the dark plate) + `--glass-bg-strong` so the wordmark stays legible over busy photos (`--text-on-media`/always-white was invisible on the light plate). (2) **Header nav** hover + active "magic pill" now uses the glass material (`--glass-item-hover`/`-active` + blur + `--glass-border` hairline + pill radius), theme-aware. (3) **Cinematic hero entrances** on first load: Home cascades kicker → headline (word-by-word, slowed via new WordReveal `delayMs` + bigger `stepMs`) → CTAs; About cascades headline → portrait → skill-chips one-by-one (each `<Reveal>` with an explicit increasing `delay`). `ScrollAnimation` now forwards `delay`/props to `Reveal`. (4) **Contact page** top padding cut (`header + 128` → `header + 48` desktop, `+64` → `+32` mobile) so the heading + form sit a comfortable distance below the header instead of a big empty band.
- **Row reveals stagger per column · chips consolidated to glass Badge · case-study prose capped (2026-06-09)** — *row content should cascade, not pop in all at once; one glass chip component; readable line lengths.* (1) **Per-column stagger:** rows reveal left→right by column index — `delay = (i % columns) × 80ms`, which **resets each row** so it never compounds across long lists. Applied to `LogoGrid`, `ImageGrid` (gallery), Archive `ProjectCard`s (2-col), and the 3 testimonials (via a new `delay` prop on `TestimonialCard`/`ProjectCard`). (2) **Chips → glass `Badge`:** `TagRow` (card variant) now renders the shared frosted-glass `<Badge>` instead of the blurless `ProjectTag`, and **`ProjectTag` was deleted** so the non-glass chip can't be used again. `Badge` gained a **`size` prop** (`sm` default / `md` = 12px for card tags). The "stack" variant (white-on-photo pills over project imagery) stays non-glass by design (on-media, not glass). (3) **Case-study prose capped** at `min(50%, 65ch)` on ≥768px / full-width on mobile, so Problem/Process paragraphs aren't edge-to-edge (`ProjectPage.module.css`). (4) **Companies logo grid gap** 4px → 8px (the tiles are translucent glass now, so they needed more separation to read as distinct).
- **Shader removed → soft in-flow mesh is the sole background (2026-06-09)** — *one calm, living backdrop without the WebGL cost.* Deleted the fixed WebGL `HeroBackground` from the app shell (`App.jsx`). The only background now is **`.meshBackdrop`** (`App.module.css`): a soft warm radial-blob mesh (dark = ember over near-black; light = warm cream). It's **`position: sticky` + `height: 100vh` + `margin-bottom: -100vh`** so it sits IN normal flow (sampleable by `backdrop-filter`) yet stays pinned to the viewport. **Why in-flow, not fixed:** `backdrop-filter` can't sample a `position: fixed` layer (GLASS §8) — the long-standing "glass doesn't frost the background" cause. It's **STATIC (no drift):** a transform drift previously scaled its box past the viewport and caused a horizontal scrollbar, and static is also the perf choice. It extends UP behind the sticky header (`margin-top: -var(--header-height)`, `height: calc(100vh + header)`) so the background fills the viewport to the very top. *(Supersedes the whole fixed-shader + grid saga below — there is no shader and no grid anymore.)*
- **Performance: cut backdrop-filter passes (2026-06-09)** — *glass blur was on ~everything; each surface re-renders on scroll = jank.* /about alone had **42 live `backdrop-filter` surfaces** (21 of them logo cells). Dropped `backdrop-filter` from **`LogoGrid .cell`** (×21) and **`ProjectTag`** (nested, so the blur was dead anyway) — they keep their translucent `--glass-bg` tint + `--glass-border` edge, so still read as glass, just without the per-element GPU pass. Reduced the blur radii: **`--glass-blur-md` 16→12, `--glass-blur-lg` 24→16** (cheaper per pass; still frosts the soft mesh). Net on /about: **42 → 21** passes.
- **Glass recipe: dark-mode tint lifted + sheen (2026-06-09)** — *dark glass was reading muddy.* Dark **`--glass-bg`** `38,38,40/.55` → **`58,58,62/.58`**; **`--glass-bg-strong`** `28,28,30/.62` → **`48,48,52/.66`**. Added **`--glass-sheen`** (a theme-aware top-edge light wash) layered with the noise on `.glass`/`.glass-panel` for a subtle catch of light. Contact form now self-animates (`<Reveal as="form">`) so its frost survives the reveal (GLASS §8 — glass must BE the reveal element).
- **Vertical-centring fix now works on iPhone — metrics baked into the font binary (2026-06-10)** — *Simon: the font-face centring fix "works on desktop, but on my phone text still sits a bit too high in buttons/tags/badges — make it work on mobile, tablet AND desktop."* Root cause: the 2026-06-07 fix lives in `@font-face` **`ascent/descent/line-gap-override`** descriptors, which **WebKit does not support** — and every iPhone browser is WebKit, so phones rendered the font's native top-heavy box (ascent 800) while desktop Chrome rendered the balanced 906. Fix: **rewrote the same balanced metrics into the font file itself** with fontTools — `hhea` (the table WebKit/CoreText reads) and `OS/2` typo metrics set to **ascent 906 / descent −177 / line-gap 294**, `USE_TYPO_METRICS` flag set, `usWin` raised to full glyph extents (clipping safety). Saved as **`BDOGrotesk-VF-balanced.woff2`** (new name = instant cache-bust on every device; old file removed; preload + `@font-face` src updated; CSS overrides kept as matching duplicates for Chrome/Firefox). Verified in-browser: the **raw binary measured without any CSS overrides** (canvas `fontBoundingBox`) now reads ascent 91 / descent 18 per 100px — byte-identical to what the overrides produce, so WebKit and Chrome finally agree. Weight axis 300–900 preserved; size unchanged (~127 KB). **If the metrics are ever re-tuned, re-bake the binary too** (see DECISIONS.md). Simon to confirm on his actual phone after deploy.
- **SF Mono retired → BDO Grotesk everywhere on the public site (2026-06-09)** — *Simon wants one typeface.* The **`.font-mono` utility now renders `var(--font-sans)`** (BDO) — converts all ~48 usages (overlines, stat labels, design-system metadata) to BDO while keeping their small meta sizing/tracking. The `--font-mono` token is left defined **only for genuine code** (the internal Playground `.code` block). See DESIGN_SYSTEM §3.
- **Design System page — editorial redesign (2026-06-09)** — rebuilt `/design-system` (`StyleGuide.jsx`) on a **specimen-card grammar** (live sample + mono-style meta row). Every **`1px solid` border removed** — separation now comes from whitespace + tonal surfaces + the spotlight. **One spacing rhythm** throughout (heading→content 48 / group 64 / section 128). **Accent-bloom cover** replaces the old graph-paper grid. **Sticky TOC** with IntersectionObserver active-section tracking + an accent tick. **Honest radius scale** (sm 4 / md 8 / pill, with a note that lg/xl clamp to 8). Type scale rebuilt as per-role specimen cards carrying size / line-height · weight · tracking metadata.
- **Button extended (2026-06-09)** — borrowed a Figma reference's **STRUCTURE only** (own brand tokens, no Figma colours/Code Connect). Added an **`outline` variant** (translucent `--glass-border` hairline, *not* a hard border), **`iconOnly`** (square; requires an aria-label), and a real colour-only **`:active` pressed state**; sm padding tokenised to `--space-12`. Backward-compatible (primary/secondary/accent/glass/ghost + the text-roll hover all preserved). Demoed on `/design-system`. See DESIGN_SYSTEM §9 / §12.
- **REVERTED the blurrable-grid change — glass must NOT show grid lines (2026-06-08)** — *Simon: "why do all the glass effects have a specific grid line effect in them now? Undo that."* The previous entry's `.gridLayer` move made **every** glass surface (cards, buttons, pills, panels) frost the grid lines — which read busy/chaotic, not clean. Reverted fully: the grid is back on the **fixed** shader (`HeroBackground` `.grid`, `position: absolute` in the fixed layer) where `backdrop-filter` can't sample it, so glass stays clean. Also reverted the same-batch recipe tweaks to their prior values (`--glass-bg-strong` dark 0.62 / light 0.56, `--glass-shadow(-lg)` inner-highlight 0.07/0.08, `--glass-noise` opacity 0.20). Net: glass is back to its known-good pre-session look. The *lesson* (glass can't blur a `position: fixed` layer) is kept in GLASS §8, reframed as "leave the grid on the fixed layer on purpose — don't re-attempt the blurrable-grid move." Verified in-browser: `.gridLayer` gone from `<main>`, grid back in the fixed shader, glass surfaces clean.
- **[SUPERSEDED by the revert above] Glass cards finally frost the grid — the REAL root cause was the fixed background, not opacity (2026-06-08)** — *Simon: "blur is not used on my glass elements — I can literally see the grid lines crisp behind them," and correctly pushed back when I first mis-diagnosed it as the smooth-shader/opacity issue.* Proven in-browser (isolating the variable): `backdrop-filter` blurs **in-flow** content but **cannot sample a `position: fixed` backdrop** — even bright boosted grid lines stayed razor-sharp behind an in-flow `.glass-panel`, while the sticky nav frosts because real content scrolls *under* it. The grid lived in the `position: fixed` `.fixedShader`, so cards couldn't blur it. **Fix: moved the grid OUT of the fixed layer into `App`'s new `.gridLayer`** — `position: sticky; height:100vh; margin-bottom:-100vh; z-index:-1` inside `<main>`, so it's in main's blurrable compositing context yet still pinned to the viewport (radial edge-mask kept). Verified: an in-flow `.glass-panel` now visibly softens the (boosted) grid behind it. The smooth shader stays fixed (a gradient has nothing to blur). Also **strengthened the glass recipe** (the levers that read "frosted" regardless of backdrop): `--glass-bg-strong` dark 0.62→**0.58** / light 0.56→**0.52**, inner top-highlight in `--glass-shadow(-lg)` 0.07/0.08→**0.12/0.15**, `--glass-noise` opacity 0.20→**0.28**. Full mental model written up in GLASS_DESIGN_SYSTEM §8 ("the fixed-background trap"). ⚠️ Over the *real* (faint, masked) grid the frost is subtle by design — Simon to eyeball the feel; lever = grid strength + recipe.
- **Company logo wall → mono silhouettes on theme-aware frosted glass (2026-06-08)** — *Simon: the company tiles looked solid (opaque charcoal in light mode), not frosted glass; "make them light or dark … and just use our light/dark glass styling."* Cause: the 21 logo PNGs were 400×400 with **fully opaque `#1e1e1e` backgrounds** that completely covered the glass `.cell`. Fix: batch-processed every logo (Pillow, luminance→alpha ramp) into a **white silhouette on transparent** so the theme-aware glass cell shows through; LogoGrid `.logo` stays white in dark mode and **`filter: invert(1)`** in light mode (dark logos on light glass). Result: clean, consistent two-tone logo wall on real frosted glass in both themes (was muddy charcoal-on-cream in light). Originals backed up to `/tmp/logos_orig`; brand colours intentionally dropped for a uniform wall (Simon's call). Verified both themes in-browser.
- **Lightbox nav arrows pinned to viewport edges — fixes mobile fast-tap close (2026-06-08)** — *Simon: on mobile, fast taps on the gallery prev/next sometimes "miss" and hit the backdrop, closing the lightbox.* Cause: the arrows were in-flow flex items hugging the image (`.row`); each `<m.img key={index}>` remounts on navigation, and a not-yet-loaded image collapsed the row → arrows slid inward → the rapid second tap landed on the backdrop (`onClick=close`). Fix: arrows are now **`position: fixed`** at the left/right viewport edges, vertically centred (`.navPrev`/`.navNext`), so they never reflow with image size/load; press-scale uses a compound `.ctrl.nav:active` so it keeps the centring translate. Hardened the backdrop too — it now closes **only when the click target IS the backdrop itself** (`e.target === e.currentTarget`), never a bubbled child. Verified: close pinned top-right, prev/next pinned + vertically centred at edges.
- **Spacing scale extended to 200 + orphan fixed (2026-06-08)** — *Simon: "update spacing to use up to 200 … check if something needs updating."* Added `--space-200: 200px` (top of the sparse large end: 128 · 164 · 200, for page/section rhythm) and `--space-56: 56px` — the latter was an **orphan** (referenced in StyleGuide.module.css via `var(--space-56, 56px)` fallbacks but never defined). Updated the live `/design-system` spacing scale (it was also stale — missing the existing 164) and `DESIGN_SYSTEM.md §4`. Verified the page shows 56/164/200 with bars fitting.
- **Glass grain de-pixelated — softer, cleaner frost (2026-06-08)** — *Simon: the grain on glass read "pixelated".* Cause: `--glass-noise` was a hard-edged per-pixel `feTurbulence` (140px tile, baseFreq 0.8, opacity 0.28) — sharp noise pixels read as blocky static on retina. Fix: added **`feGaussianBlur stdDeviation='0.6'`** after the turbulence (softens hard pixel edges → smooth micro-grain — the key lever), grew the tile **140→200px** + octaves **2→3** (finer, less obvious repeat), opacity **0.28→0.20** (present-but-subtle). Same soft-light blend on `.glass`/`.glass-panel`, filter id `gn`. **Tested quantitatively** (drew each noise to canvas, measured adjacent-pixel difference = a pixelation proxy): blur sweep showed a knee — full-opacity harshness 12.5 raw → ~10 at blur 0.4 (too gentle, was the first attempt) → **4.3 at blur 0.6** → 2.2 at 0.8. Landed 0.6 (smooth, still grainy not cloudy). LIVE on-screen harshness now **0.87** (vs first-ship 1.62, vs original hard ~3.0); deployed token verified end-to-end + glass renders clean (no seams). Knobs: opacity = strength · blur = smoothness · baseFrequency = grain size. *(Separate global film-grain `.grain` (id `n`) over the whole page is untouched — if pixelation persists it's the second candidate.)*
- **Glass cards now frost IMMEDIATELY on scroll-reveal — glass element IS the reveal (2026-06-08)** — *Simon: "blur not appearing immediately on scroll loads."* Investigated the DOM: the prominent glass cards were **wrapped** by the reveal (`<Reveal>`/`<ScrollAnimation>` → glass child), and a wrapper animates **opacity AND transform** — *both* isolate `backdrop-filter` (opacity<1 is a backdrop-root trigger too), so the child's frost was dead for the whole reveal then popped in. Key realization: **`REVEAL_SHIFT` (quick-transform decouple) can't save a wrapped child** — the wrapper's slow opacity fade keeps isolating it. Fix = the glass element must **BE** the reveal (own opacity composites the already-frosted element → frost visible from frame one; own transform is fine). Refactored the four offenders so the glass element animates itself: **ProjectCard** (`<Link>` → module-scope `m.create(Link)` passed as `Reveal as=`), **SkillCard** (`<Reveal className="glass-panel …">`), **TestimonialCard** (`<Reveal as="figure">`), **LogoGrid cell** (`<Reveal>`); extended `<Reveal>` to accept a custom motion component for `as`. Verified in-browser (animations force-finished): all four now `self-animating, wrapped=false`; every visible glass keeps `backdrop-filter` active; build + lint clean; zero console errors across home/archive/about. **Residual (accepted, documented in GLASS §8):** nested glass — the tiny tag pills *inside* project cards still lose frost during the card's ~0.5s reveal (a glass child of an animating glass card can't keep frost — unavoidable); and WorkedAt hover-tooltips read as "wrapped" but are hidden until hover (shown only after the reveal settles → fine). ⚠️ Frost-feel needs Simon's real-browser eyes (preview tab is backgrounded → animations pause).
- **Motion Phase 2b — reassessed: route transitions + desktop dropdown STAY NATIVE (2026-06-08)** — *Investigated the remaining "move to Motion" candidates; the honest engineering call is to leave them native — Motion would add risk to the glass system for no gain.* ① **Route/page transitions** (`.page-enter-nav` + `pageWipe` curtain) — a deliberate, working signature CSS animation (marimba-style seamless same-colour cover→reveal); Motion's strengths (springs/exit/gestures/FLIP) don't improve a curtain sweep, and the wrapper is blur-sensitive (already fixed once via `fill-mode: backwards`). Keep. ② **Header desktop dropdown** — **must stay transform-free**: it's positioned by JS left-edge (not `translateX(-50%)`) *specifically* because any transform on the wrapper kills the glass panel's `backdrop-filter`. Motion animates via transforms → would re-break the dropdown frost. Keep native (it already opacity-animates open+close). **DO NOT convert these to Motion.** ③ **Mobile menu** — the one real gap: `{isMobileMenuOpen && …}` unmounts **abruptly** on close (CSS has enter stagger `menuRowIn`, no exit). Optional AnimatePresence opacity-exit could fix it (glass-safe, contained) — parked pending Simon's call. **Net: the Motion migration is essentially complete after Phase 1 (reveals) + 2a (modal/lightbox exits).** Cross-screen verification (this session, preview capped ~745px + backgrounded so motion-feel needs real-browser eyes): all 9 routes render, **zero console errors**; **no horizontal overflow** at 745 & 375; visible glass keeps `backdrop-filter blur(16/24px)`; **settled Motion reveals reset `transform:none`** (don't kill descendant blur — glass system healthy); footer blur intact; mobile menu opens (27 items). Build + lint clean.
- **Motion Phase 2a — exit animations via AnimatePresence (2026-06-07)** — *The headline reason a library helps: the owned CSS layer couldn't animate elements OUT before unmount (they just vanished or needed manual `setTimeout` hacks).* Converted the two clean overlay cases to **Framer Motion `<AnimatePresence>`**: ① **Lightbox** (`ImageGrid` — About "Pictures") — dropped the manual `closing` state + `setTimeout(260)` choreography; backdrop now `m.div` (opacity in/out, keeps its `blur(8px)`), image `m.img` (scale+fade, keyed by index so it re-plays on prev/next). ② **Modal** (`overlays/Modal`, the ready-but-unwired dialog primitive) — overlay is an `m.div` propagating `open`/`closed` variants to a scrim (fade) + dialog (fade + slight rise/scale), enter **and** exit; all a11y machinery (focus-trap, scroll-lock, Esc, focus-restore) untouched. Removed the now-dead CSS `@keyframes` (lightboxIn/Out/ImgIn/Out, scrim-in, dialog-in) + `.lightboxClosing` so CSS can't fight Motion's inline styles; reduced-motion handled in JS via `useReducedMotion`. Verified: build + lint clean; lightbox **enter** confirmed visually in-browser (full image/arrows/counter/scrim render). ⚠️ **Exit-unmount not time-verifiable here** — the preview tab runs `visibilityState:hidden`, which pauses WAAPI/rAF so AnimatePresence's exit-complete never fires (stuck mounted in-probe); the pattern is canonical (keyed direct child + `exit` prop) and runs in a real visible browser — **Simon to confirm close/reopen feels right**. **Deferred (Phase 2b, blur-sensitive + high-visibility, own focused pass): route/page transitions + Header dropdown/mobile-menu.** Skipped (CSS is right tool): hover/press, count-up; ScrollProgress left exact-tracking by design.
- **Typeface vertically balanced at the source — `@font-face` metric overrides (2026-06-07)** — *Simon: text in buttons/pills/nav wasn't vertically centred — sits high with extra space below; and "why not fix it across ALL uses of the typeface?"* Root cause is the font's metrics, not the components: **BDO Grotesk ships a top-heavy box** — native ascent **800** / descent **177** / cap-height **729** (per 1000 em), so its line-box centre (311 above baseline) sits *below* the caps' optical centre (365), and `align-items:center` therefore parks the letters **~5.5% of font-size too high** everywhere. Chose the **global** fix (over per-component `text-box-trim`): rewrote the metrics on the `@font-face` rule so the box is balanced for **every** use. A box centres caps when **`ascent − descent = cap-height`**, so **`ascent-override: 90.6%`** (906) / **`descent-override: 17.7%`** (177, held at the descender ink so g/j/p/y never clip) / **`line-gap-override: 29.4%`** (keeps `line-height: normal` at its original ~1.377). Verified in-browser: ascent−descent = 729 = cap-height exactly; caps now centre to **~0.1px** in fixed-height buttons/pills (was ~0.8px high at 14px). **Knock-on (inherent to the font-level route):** the balanced box is **1.083em** tall (descender-depth bound), so multi-line display type below that overlaps — raised the sub-floor display line-heights to **1.1**: `.type-display` (desktop 1.0→1.1, sm 1.05→1.1), `.type-display-sm` (lg 1.05→1.1), About `.aboutHeadline` (1.08→1.1). Single-line stat numbers at `line-height:1` left as-is (can't overlap); the testimonial quote glyph is Georgia (unaffected). Verified no real multi-line overlap at 1440 **and** 375 (hero wraps at 1.1, clears the floor); build + lint clean. **The desktop display headings are now a hair more open (1.1 vs the old 1.0–1.05) — a one-line revert if Simon wants them tighter. Simon to eyeball the centring + hero leading in his real browser.**
- **Scroll-reveal → Framer Motion spring (2026-06-07)** — *After hand-tuned CSS easing couldn't land the feel, Simon chose to adopt a motion library specifically for quality; picked the "Calm" spring in the side-by-side lab.* Adopted **Motion (Framer Motion, `motion/react`)** — installed `motion`, wired `<LazyMotion features={domAnimation} strict>` at the App root (features lazy-load as a ~14kb chunk, out of initial paint; use `m.*`). Reveal feel is one source of truth in **`src/components/motion/revealMotion.js`** — Calm spring **stiffness 55 / damping 18 / mass 1**, travel **28px**, stagger **90ms**, presets fade-up/fade/scale-in. **`<Reveal>`** now renders `m[as]` animating `hidden`→`shown`, **`<Stagger>`** injects a per-child `delay` (ms) instead of the old CSS `--i`, and **`<ScrollAnimation>`** just renders `<Reveal>` — so all three share the one spring and can't drift. **Trigger kept on our `useReveal` IntersectionObserver** (rootMargin `-28%`) on purpose — preserves the dialled-in trigger position rather than switching to Motion's `whileInView`. Reduced-motion: starts `shown`, duration 0 (content never depends on motion to be visible). Built a **`/motion-lab`** page (unlinked) with live spring sliders + replay for tuning. Verified in-browser (home): revealed els settle to `opacity 1 / transform none`, below-fold held at `opacity 0 / translateY(28px)`, no console errors; build + lint clean; Motion chunk split (14kb gzip). Best-practices written up in DESIGN_KNOWLEDGE §4.9; decision in §4.11. **Phase 1 = reveals only**; transitions/modal/lightbox/menus/count-up are Phase 2. ⚠️ **OPEN CAVEAT:** the spring animates the `translateY` glide on the *same* (slow) Calm spring, so on glass cards the **frost-pop-in this entry's predecessor fixed may return** (non-zero transform suppresses `backdrop-filter` until the spring settles ~1s). Options if it shows: per-property transition (quick spring/tween on `y`+`scale`, calm spring on `opacity`) — mirrors the prior CSS fix while keeping the slow fade. Awaiting Simon's eyes.
- **Frost no longer "pops in" after the scroll-reveal (2026-06-07)** — *Simon noticed the blur only appeared once the reveal motion finished.* Cause: `backdrop-filter` is suppressed while an element is `transform`ed, and the reveal animates a `translateY` rise on the card wrapper — so for the whole slow reveal the card was a backdrop-root (blur dead), then the frost snapped on when the transform settled. Fix (Simon's pick: keep the glide, shorten the transform): split the reveal so the **positional glide is quick** (new `--motion-reveal-dur-shift: 0.42s`) while **opacity stays slow** (~0.9s). The transform reaches `none` early (frost on) while the card is still mostly faded-out, so the frost is present for the visible part of the reveal instead of popping in at the end. Applied globally on `.fx-reveal` (opacity keeps `--motion-reveal-dur`, transform uses the new shift duration). Verified computed transition = `opacity 0.9s, transform 0.42s`.
- **Cards now read as frosted glass, not solid panels (2026-06-07)** — *Simon: "cards aren't using blur, but the footer is."* Diagnosis (verified): the cards' `backdrop-filter: blur(24px)` WAS active with no blur-breaking ancestor when revealed — but `--glass-bg-strong` was so opaque (dark 0.80 / light 0.66) that over the *smooth* shader the frost was invisible and they read as flat panels, whereas the footer (`--glass-bg` 0.55/0.46) let the shader show through and read "glassy." Fix: lowered `--glass-bg-strong` → **dark 0.62 / light 0.56** so the warm shader frosts through the cards (and dropdowns/tooltips/modal) while keeping enough scrim for text. Verified both themes: card culprit-ancestors = none, blur(24px) active, shader visibly bleeding through. (Real lesson: over a smooth backdrop the "frosted" look comes from translucency + tint + noise, not the blur itself — so panel opacity is the lever.)
- **Blur was silently dead on most components — fixed (2026-06-07)** — audited every `.glass`/`.glass-panel` on every page (walked ancestor chains for transform/filter/will-change/contain — the things that make an ancestor a "backdrop root" and kill descendant `backdrop-filter`). Two systemic killers: ① **`.page-enter-nav`** route-transition wrapper animated to `translateY(0)` with `animation-fill-mode: both` → left a permanent identity transform around the WHOLE page → every glass surface on every navigated page blurred *nothing*. Fix: `both`→**`backwards`**. ② **Nav dropdown `.panel`** used `transform: translateX(-50%)` to self-centre (even open) → killed the open menu's blur. Fix: position by **left-edge from JS** + open/close animates **opacity only** (no transform). Verified timer-independently (via `getAnimations().finish()`): page-enter-nav + revealed `.fx-reveal` settle to `transform:none`; dropdown panelCard now `blur(24px)`, wrapper transform `none`. Remaining intentional non-blur: project **stack cards** scale on scroll, so their `+N` tag tooltips don't blur (accepted). *(Local preview throttles JS timers → reveal/transition states look "stuck" mid-animation in probes; trust resting-state checks.)*
- **Glass noise dialled down** — `--glass-noise` rect opacity `0.55`→**`0.28`** (Simon wanted less); subtler whisper of frost.
- **Frosted-glass noise → Apple "Liquid Glass" (2026-06-07)** — *Simon wanted the whole system to feel genuinely Apple-frosted, not just blurred.* Baked a **per-panel noise texture** into every glass surface: new **`--glass-noise`** token (a faint grayscale `feTurbulence` fractal-noise tile, unique filter id `gn` so it never clashes with the global `.grain`'s id `n`) applied to `.glass` + `.glass-panel` as a `background-image` blended **`soft-light`** over the translucent fill — a whisper of sandblasted grain on top of the existing blur + saturate + border highlight + shadow. Chose a background-layer (not a `::before`/`::after`) deliberately: the tooltip hover-bridges already own pseudo-elements on `.glass-panel` elements, so a pseudo would collide. Dropped in the solid/a11y fallbacks (`@supports not backdrop-filter`, `prefers-reduced-transparency`, `prefers-contrast` → `background-image: none`). Because it lives on the shared `.glass`/`.glass-panel` classes, ALL glass inherits it automatically (nav, cards, dropdowns, tooltips, modal, badges, chips, logo tiles). Verified both themes in-browser: noise + `soft-light` + blur all active; light cards (warm shader frosts through) and dark cards (28,28,30/.8 fill) both read as clean frosted panels, no "dirt".
- **Scroll-reveal slowed + relaxed (2026-06-07)** — *Simon: every scroll-appear felt "too early and too fast", and the opacity itself revealed too fast (his calm-motion DNA, §6.7/§6.8).* Retuned the central `.fx-reveal` system (covers all `<Reveal>`/`<Stagger>` usage site-wide) rather than per-page: ① **duration `--dur-slow` (450ms) → new dedicated `--dur-reveal: 900ms` (landed on 900 after 450→too fast, 820→too fast, 1200→too slow)** (decoupled from `--dur-slow`, which many other components share); ② **travel** — `--motion-shift-md` 16→**28px** (sm 8→14, lg 24→40) so the glide reads as movement, not just a fade; `fade-up` (opacity+translateY) is the default preset so both channels animate together; ③ **split the easing** — *movement* keeps `--ease-decelerate` (glides in, settles softly) but *opacity* gets its own **`--motion-reveal-ease-opacity: cubic-bezier(0.45,0,0.5,1)`** symmetric ease-in-out so the fade ramps evenly across the full duration instead of front-loading to ~90% in the first third (the decelerate curve was the real cause of "opacity too fast"); ④ **fires much later** — `useReveal` rootMargin `-10%`→**`-28%`** bottom + threshold 0.12→0.1, so it only starts once the element is well into view; ⑤ **stagger relaxed** `--stagger-1..4` 60/120/180/240 → **90/180/270/360ms**. Verified in-browser: computed transition = `opacity 0.9s cubic-bezier(.45,0,.5,1), transform 0.9s cubic-bezier(.16,1,.3,1)`, start state opacity 0 / translateY 28px. Reduced-motion net unchanged (instant, no hide). Grounded in calm-editorial best practice (Apple/Linear/Stripe/Awwwards): long even-fade reveals, gentle travel, trigger when comfortably in view. *(Note: the local preview env throttles JS timers, so scrub-by-`setTimeout` opacity traces are unreliable here — trust the computed transition + real-browser eyeballing.)*
- **Scroll-reveal UNIFIED — one system site-wide (2026-06-07)** — *Simon: content "appearing different from page to page" — cards etc. faded in differently per page.* Root cause: **two parallel reveal systems** had drifted apart — the central `<Reveal>`/`.fx-reveal` (which I'd just slowed) **and** a separate `<ScrollAnimation>` wrapper (`components/animations/`) on its own 600ms/decelerate/16px/`-50px`-trigger config, used by **About, Archive, SkillCard, TestimonialCard, LogoGrid, ExperienceTimeline, StyleGuide** (About even mixed both). Fix: **collapsed `<ScrollAnimation>` into a thin alias over the same `useReveal` hook + `.fx-reveal`/`.fx-fade-up` classes** (deleted its now-dead `.module.css`) → single source of truth, can't drift again. Verified in-browser: all reveal elements across Home/About/Archive/Project (80 total) report the **identical** computed transition `opacity 0.9s cubic-bezier(.45,0,.5,1), transform 0.9s cubic-bezier(.16,1,.3,1)`. Build + lint clean. *(Note: the preview harness reports `innerHeight:0` at mount + clamps programmatic scroll, so reveals must be eyeballed in a real browser — confirmed they fire on scroll there.)*
- **Frosted-coherence pass + new components (2026-06-07)** — swarm-audited the whole system (~95% glass-coherent already) and closed the gaps:
  - **Form fields → glass** (Contact `.input`/textarea + reusable `Input`): translucent fill (`--glass-item-hover`) + `backdrop-filter` blur + `--glass-border` hairline + soft accent-glow focus — kills the last "normal form" surface (was solid grey + hard border).
  - **Softened remaining hard borders/boxes**: ThemeToggle (border→inset glass shadow; hardcoded shadow→`--shadow-xs`), ProjectCard `.impact` badge (→glass pill), CookieConsent "Decline" (→`--glass-border`), RecognitionStrip placeholder (dashed→opacity).
  - **Keyboard focus everywhere**: new **`.focus-ring-glass`** utility (soft accent glow, no hard outline) + `.glass-item:focus-visible`; applied to ThemeToggle / footer links / header nav triggers / Worked-at logos. Light `--glass-item-active` `dark-8`→`dark-12` (was nearly invisible); Footer blur `lg`→`md`.
  - **NEW `Badge`** (`components/Badge.*`): glass pill — variants solid/outline/active × tones neutral/accent; showcased in the style guide.
  - **NEW `ScrollProgress`** (`components/ScrollProgress.*`): 2px fixed accent reading-progress bar on case-study pages; rAF-throttled, reduced-motion safe.
  - **NEW `Modal`** (`components/overlays/Modal.*`): accessible frosted dialog primitive (portal, `.glass-panel`, scrim, Escape, scroll-lock, focus-trap+restore, `useId`, calm motion). Standalone/ready — not yet wired in.
- **Charts / KPI kit (2026-06-07)** — *new `src/components/charts/` kit so the system can present stats/graphs for SaaS-style work, not just a portfolio (follows the discovery in DESIGN_KNOWLEDGE §6.10).* `StatCard` (glass KPI: label → count-up value → delta-with-arrow → optional sparkline; `accent` reserves the warm accent for one hero metric), `StatGrid` (responsive auto-fit + stagger), `Sparkline` and `TrendChart` — all **hand-rolled SVG, zero dependencies**, token-driven, editorial (no gridlines/boxes/legends; accent gradient fades to transparent so glass frosts through; direct end-labels; `tabular-nums`). New `useCountUp` hook (easeOutCubic, reduced-motion shows final value). Delta uses arrow + sign (never colour-alone, a11y). `TrendChart` is **interactive** — hover/touch or focus + ←/→/Home/End/Esc moves a vertical guide + highlighted point with a value tooltip (`labels`+`format` props), active point announced via `aria-live`; tooltip is a solid surface (not glass) to avoid glass-on-glass. Demo added to `/style-guide#data` (new TOC entry). Verified: build + lint clean; DOM/SVG geometry, glass surface (`backdrop-filter blur(16) saturate(1.8)`, 8px radius, spotlight glow) and `tabular-nums` confirmed in-browser. NOTE: the live count-up/draw-in is reveal-gated (IntersectionObserver) and the local preview env reports `innerHeight: 0`, so the animation itself must be eyeballed in a real browser at `/style-guide#data`.
- **Design-systems research pass (2026-06-07)** — *studied Apple HIG/Liquid Glass, Material 3, Geist, Linear, Stripe, Carbon & Polaris; applied 3 additive, low-risk wins (the rest validated what we already do — see DESIGN_KNOWLEDGE §6.10).* ① **Tonal surface ramp** — new `--surface-1..4` tokens + `.surface-1..4` utilities per theme: elevation by a lightening/deepening *tone*, not shadow/border (M3/Linear) — reserve shadow/glow/glass for the topmost layer only. ② **`--accent-soft` now OKLCH-derived** (`color-mix(in oklch, var(--accent) N%, transparent)`) instead of a hardcoded `brand-500` rgba — fixes a light-mode mismatch (light `--accent` is `brand-600`) and keeps the warm tint perceptually uniform across themes. ③ **Type micro-craft** — `text-wrap: balance` on display/heading/title roles, `pretty` on subtitle/body roles, `text-box-trim: trim-both` on buttons (`@supports`-guarded) — wires §7.1's documented craft into the actual `.type-*` classes. Verified in-browser (light): hero balances to 3 even lines, accent-soft resolves to `oklch(…/0.12)` off brand-600, text-box-trim active. Already-in-place (no change): global `.grain`, on-media `.stackScrim`, `font-optical-sizing`, reduced-transparency/contrast/motion fallbacks.
- **Chips/tags → glass pills (2026-06)** — `ProjectTag` is now a `--radius-pill` chip with translucent `--glass-bg` + `backdrop-filter` blur + `--glass-border` (theme-aware), replacing the flat `bg-surface-color-tertiary` rounded-md chip. Same frosted material as the rest of the glass system. Design system updated.
- **Glass coverage fixes** — client/brand logo tiles (`LogoGrid .cell`) now use the glass material (translucent + blur, theme-aware) instead of an opaque tile; the nav active "magic pill" (`.navPill`) gets `backdrop-filter` blur on its transparent `--glass-item-active` tint.
- **Cards: tags pinned to bottom** — project-card tag row uses `margin-top: auto` so chips align to the card's bottom edge across a row (verified equal bottom Y on Archive).
- **Grids min-width (no cramping)** — testimonials, About skills, and the photo gallery switched to `repeat(auto-fit, minmax(MIN, 1fr))` (340 / 240 / 200px) so columns wrap to fewer instead of squishing.
- **Mobile menu = glass + accordion** — the mobile nav is a frosted `glass-panel` (blur, both themes); Projects/Archive/About are now **accordion sections** that expand (grid-rows 0fr→1fr, reduced-motion safe, scrollable) to reveal the SAME cards/rows as the desktop dropdown for that page; Contact stays a direct link. Verified: menu visible, cards render in-view with thumbnails.
- **Tight radius (2026-06)** — capped the radius scale: `--radius-lg/xl/2xl/3xl` all = **8px** (sm 4, md 8, pill unchanged). Cards/panels/images now read tight (≤8px) site-wide from one token change instead of a per-file sweep. (Images already mapped to `--radius-md` via `Media`.)
- **Hover "lift" removed → cursor spotlight on ALL glass cards** — dropped the `translateY` lift (felt odd). New shared system: global **`.fx-spotlight`** glow overlay + **`useSpotlight`** hook (`src/hooks/useSpotlight.js`) writing `--mx`/`--my`; cards add `group` + `position:relative; overflow:hidden` + `<span class="fx-spotlight">`. Glow is theme-aware via **`--glass-glow`** (dark = white specular `--transparent-light-12`; light = soft warm `color-mix(accent 16%)` since a white glow is invisible on light cards). Applied to ProjectCard, TestimonialCard, SkillCard.
- **Light-mode glass now actually frosts** — light glass was too opaque (`--glass-bg-strong` 0.85 / `--glass-bg` 0.60) so it read as flat white. Lowered to **0.66 / 0.46** so the warm shader blurs through and reads as glass; dark text stays legible. Verified blur(24px) active + warm tint visible on cards in light mode.
- **Nav bar** — removed its border and set **max roundness** (`--radius-pill`) — a cleaner floating stadium bar. **Footer** — removed its top border. **Archive grid** — equal gaps (`gap: var(--space-24)`, was 24/64).
- **Hover bridge on tooltips (2026-06)** — both the Worked-at tooltip and the reusable `Tooltip` now render an invisible `::before`/`::after` strip spanning the gap between trigger and popup, so the pointer can travel into the tooltip without crossing a dead zone that drops `:hover` and dismisses it. Pattern name: *hover bridge / hit-area buffer* (a form of hover-intent; cf. Amazon's "safe triangle").
- **Testimonials levelled up (research-backed)** — quotes are now **arrays of paragraphs** (natural reading rhythm, no wall-of-text); first paragraph is a brighter **lead line** (`--text-color-primary`); one restrained **oversized accent quotation glyph** top-right per card (the section's single warm-orange touch, opacity 0.22); Frej Korsgaard testimonial switched to the **Adtraction** logo + company (was Adservice image). Kept the 3-up glass cards. Further option parked: a *featured + grid* asymmetric layout (one oversized pull-quote) — the biggest lever per the research, not yet done. Refs added to DESIGN_KNOWLEDGE §6.
- **Dropdown polish** — Archive dropdown list keeps an **always-visible scrollbar** (styled `::-webkit-scrollbar` + Firefox `scrollbar-color`, `overflow-y: scroll`) so the scroll affordance is obvious; dropdown **row hover-highlight radius tightened** `--radius-xl` → **6px**.
- **Responsive QA (mobile 390 / tablet 768)** — verified no horizontal overflow across Home/Archive/Project/Contact. Fixes: case-study **meta grid is mobile-first 2-col**, widening to 4-up only at **≥1024** (was a fixed 4-col that crushed values like "Lead Product Designer" into ~57px cells); the 50%-width Problem paragraph is **full-width below 768px**.
- **Reusable `Tooltip` component (2026-06)** — `src/components/Tooltip.{jsx,module.css}`. Pure-CSS hover/focus glass tooltip extracted from the Worked-at pattern: `.glass-panel` popup, `placement="top|bottom"`, fade+translate on `:hover`/`:focus-within` (`--dur-base`/`--ease-decelerate`), reduced-motion safe. Use it for any hover tooltip going forward.
- **Worked-at LOGO BALANCE rule** — every logo must read at the same *optical* size, not the same pixel height. Each company has a `logoScale` multiplier on the shared base height, tuned in-browser. Current: Lenus 1 · Beefit 1.12 · Zliide 0.7 · Adtraction 0.9. Rule documented in `WorkedAt.jsx`. Beefit also got its real lockup logo + `beefit.io` website; Zliide industry = Fashion technology.
- **Tag overflow "+N" now has a styled tooltip** — `TagRow` extracted to `src/components/projects/TagRow.jsx` (shared by Archive `ProjectCard` *and* front-page stack cards via a `variant="card|stack"`). The "+N" chip shows the hidden tag names in the new glass `Tooltip` on hover (replaced the native `title`). Stack-card tags now collapse/overflow identically.
- **Project case-study page** (`ProjectPage`) — meta row is now a **4-col grid** (Role · Client · Team · Timeline, 25% each); **Outcome temporarily removed**. Body text fills **100% width**; the opening Problem paragraph is capped at **50%** (`.problemLede`).
- **Contact page** — centered; headline → **"Get in touch with me"**; removed the lede + the "Or reach me directly" block (email/LinkedIn/Location).
- **Lightbox arrows** — now sit in a centered flex row with the image, **16px gap** each side (was viewport-pinned). **Nav "Aarhus"** removed from the profile dropdown subtitle (kept on the live location chip).
- **Glass audit (2026-06)** — swept all components/pages for hardcoded transparent backgrounds.
  Result: `backdrop-filter` already tokenised everywhere (Footer/ImageGrid/glass classes). The only
  offenders were the **project stack-card overlays** (logo/impact/tags/title) using hardcoded
  `rgba(255,255,255,*)` / `#fff` / `#ff9a6a`. Replaced with tokens: new **`--transparent-light-90`**,
  **`--text-on-media: #fff`**, **`--accent-on-media: #ff9a6a`** (theme-independent — these always sit
  over dark-scrimmed photos). *Deliberately NOT given `backdrop-filter`*: they live inside a card that
  scales on scroll (blur would jank and the transform breaks it anyway). Shader colour arrays,
  StyleGuide swatches, the image scrim gradient, and `var()` fallbacks are legit raw values, left as-is.
- **Testimonial cards now actually frost** — they always used `.glass-panel`, but their `ScrollAnimation`
  wrapper rested at `transform: translateY(0)`; any non-`none` transform creates a containing block that
  **kills `backdrop-filter` on descendants**. Changed `.visible` to `transform: none` → blur renders. (Fix
  applies to every glass surface revealed via ScrollAnimation.)
- **Project-card tag overflow now correct** (`ProjectCard.jsx` `TagRow`) — only shows the trailing
  **"+N"** chip when tags genuinely don't fit; collapses the exact overflowing tags otherwise. Two bugs
  fixed: (1) the real cause — `.hiddenChip { display:none }` sat *before* `.tagSlot/.plusTag { display:
  inline-flex }`, equal specificity so the later rule won and nothing ever hid (phantom "+1"); now a
  compound `.tagSlot.hiddenChip`/`.plusTag.hiddenChip` selector. (2) refactored TagRow to a single
  `count` state (chips + "+N" derive from it) so a re-render can't desync from imperative hiding;
  re-measures after `document.fonts.ready`.
- **No Tailwind; CSS Modules + tokens** — owned, portable styling; primitives → semantics (two-tier).
- **Testimonials grid narrow-phone fix** — `.testimonialsGrid` used `minmax(340px, 1fr)`, whose 340px floor can't shrink, causing ~44px horizontal overflow on phones ≤~388px wide (the global fixed shader then stretched to match, so the *whole page* scrolled sideways). Changed to `minmax(min(340px, 100%), 1fr)` so the column collapses to the container below 340px. *Found in the full-device responsive audit; verified 320/360/375/768/1440/1920 all overflow-free after.*
- **Responsive page gutter** `--page-gutter`: 24px (mobile) → 40px (≥640) → **80px (≥1024)** — *a flat large gutter overflowed phones; scales with viewport. Bumped 64→80 on desktop (Simon keeps wanting more breathing room).*
- **Standard section gap = 164px** (`margin-bottom: var(--space-164)`) across ALL pages (Home/Archive/About/Playground/case-study) — iterated 80 → 128 → 164 for a generous, consistent vertical rhythm.
- **Global shader background** — the flow-gradient shader is now a **single `position: fixed`, full-viewport layer behind ALL content on every page** (in the App shell, z-index 0; content at z-index 1+). It stays put while the page scrolls over it. Removed the per-hero and per-footer `HeroBackground`; the footer is now transparent. *Why: one consistent, living backdrop sitewide.*
- **Primary typeface = BDO Grotesk** (LCTipografi, SIL OFL 1.1), self-hosted variable font at `public/fonts/BDOGrotesk-VF.woff2` (single `wght` axis 300–900). Replaced Inter sitewide via the `--font-sans` token; removed the Google Fonts `<link>` (no more third-party request) and added a `preload`. *Why: a neutral Swiss neo-grotesque much closer to PP Neue Montreal's tight, confident editorial character than Inter's open-aperture UI feel — chosen after a fact-checked search of free variable grotesques (BDO was the verified standout: indie, OFL, genuinely variable). Weight values unchanged — Regular 400 / Medium 500 etc. map onto the same axis.*
- **Optical sizing** — `font-optical-sizing: auto` is still set globally but is a **no-op today**: BDO Grotesk is weight-only (no `opsz` axis). Kept so any future opsz-capable font is mapped per size. *(Previously Inter was requested with `opsz,wght@14..32,100..900`.)*
- **`.fx-reveal.is-visible` releases `will-change` (→ auto)** — keeping it left a containing block that broke `backdrop-filter` on descendants (the Worked-at tooltip had no blur); also a perf best-practice (promote then drop).
- **`--header-height: 88px`** — used to slide the hero shader up behind the sticky nav.
- **Type scale 10–48** with semantic roles (`.type-display`…`.type-overline`); **added `.type-micro` (10px)** — *badges/footnotes/dense meta.*
- **Graduated negative tracking — deliberately tight** (Simon's preference, "all between −2% and −4%, maybe more"): display **−5%**, headings **−4%**, titles/body **−3%**, small **−2.5%**; overline stays **+8%** (caps need positive). Hardcoded hero + About headlines also set to −5%. *Intentionally overrides the usual "body ≈ 0 / small text slightly positive" best practice — Simon wants the dense, tightly-set look across the whole ramp. Do NOT loosen it back.*
- **Status primitive ramps** green/amber/red/blue (50–950) + brand orange + transparent light/dark — shown in the style guide.
- **Image corner radius standardized to 8px** (`--radius-md`) across all content imagery (via `Media`) — *one consistent rounding.*
- **`scroll-behavior: smooth`** on `html` (reduced-motion → auto) — for in-page anchors like "View projects".
- **`--z-intro: 2000`** — intro loader above all chrome incl. grain.
- **Page-transition + content-reveal keyframes** live here (`pageWipe`, `page-enter-nav`).

## 1. Header / Nav (`src/components/Header.*`, `Location.*`, `ThemeToggle.*`)
- **Brand (avatar + name) links to home** with a subtle hover bg — *expected affordance.*
- **Location dot is success-green** + soft pulse (was orange brand) — reads as "available".
- **Sliding "magic pill"** travels smoothly between Projects/Archive/About/Contact and rests on the active route; **per-button hover background removed** so only the pill animates — *one effect, not two.*
- **Dropdown project thumbnails**: border-radius → **4px** (`--radius-sm`).
- **Dropdown titles removed** (no more "Featured work" / "From the archive" labels); **Archive link removed** from the Projects dropdown footer.
- **Archive dropdown list scrolls internally** (max-height 46vh, **scrollbar always shown**) so the **"View full archive →"** footer stays pinned/visible.
- **Dropdown menus**: **flexible width** (`max-content`, capped `min(92vw,560px)`) so they fit content and adapt; **single-column** content; **whole menu staggers** header → items → footer (one-by-one reveal); **close is scheduled on leaving the whole nav region** (not each button) so moving across gaps keeps it open and the active trigger highlighted.
- **Bug fix — "Worked at" label was invisible**: the hero's absolutely-positioned bottom **fade overlay** painted over the (statically-positioned) label, hiding it while the logos below stayed visible. Fixed with `position: relative; z-index: 1` on the Worked-at section. *(Lesson: an absolutely-positioned overlay paints above later static siblings — give overlapping content its own stacking context.)*
- **Tooltip spacing**: 12px padding, 12px gap below the logo, 20px label→value gap.
- **Nav triggers (Projects/Archive/About/Contact) get the button text-roll** on hover — same effect + timing (0.55s easeInOutQuart) as the buttons, layered with the sliding pill.
- **Contact dropdown**: title "Contact me" only (removed "Let's work together / Open to new projects and roles"); footer shows green dot + **"Aarhus, Denmark · <live time>"** (removed "Available").

## 2. Home — Hero (`src/pages/Home.*`, `HeroBackground.*`, `WordReveal.*`)
- **SHIPPED — the halo hero goes live (2026-09-05, end of the exploration):** Simon's call: *"Lets have the halo version go live but save the sky thing for iterations for us."* The exploration scaffolding is gone from production and the hero is now a real component, **`components/home/hero/Hero.jsx`** — the craft-passed layout over the **halo** light-field. What actually shipped, versus the old inline hero in `Home.jsx`: **(1) tonal emphasis inside the headline** (the connective phrase "with five years of experience across" drops to `--text-color-secondary`, so "Product designer" and the industries read first — the Linear device Simon picked in round 1, kept at the existing 32/36 size); **(2) spacing regrouped by Gestalt proximity** — headline→lead **20px** (one line of the lead's own leading), lead→CTAs **32px** (24 on phones), replacing the flat 8/16 that made the three blocks read as one undifferentiated stack; **(3) the lead gets a ~62ch reading measure** instead of running the full 513-box at ~84 characters a line; **(4) the halo Atmosphere** behind it (dark theme only — light stays a flat white ground, per Simon: "remove the white version"). `Home.jsx` lost the whole inline hero and its now-dead CSS (`.hero`/`.heroInner`/`.headline`/`.lead`/`.ctaRow` + their media queries) — `.container` stays, the other sections use it. **The parked backdrops stay one click away without shipping:** `HeroExplorer.jsx` (switcher + aurora/sky/sky-24h + the hour scrubber) renders only behind `import.meta.env.DEV`, which is a compile-time constant — **verified the production bundle contains no sky shader and no switcher CSS** (`grep` for `u_turb`/`Rayleigh`/`scrubber` → 0 hits; bundle 373 → 362 kB). Also live: the **footer wordmark** ("Simon Knudsen" at `clamp(48px, 12.2vw, 176px)`, −4.5% tracking, scroll-revealed) — the identity-scale name from round 1, in the place Simon wanted it. Verified at 1440 and 375, both themes: no horizontal overflow, halo present in dark / absent in light, mobile headline at 24px. Design System page gained an **Atmosphere · hero light-field** specimen (Materials), spec in DESIGN_SYSTEM §6.
- **Hero exploration round 4 — Sky 24h goes physically based (2026-09-05, same branch):** Simon's verdict on round 3: **Sky 24h is the winner**, "make it more dynamic to change each hour", research more beauty, **keep Halo as the backup** (dark-theme only — the light "white version" is cut). Root cause of the flatness: five hand-picked colour keyframes meant 10:00 ≈ 14:00 and morning mirrored evening. Fix: **`SkyField.jsx` v2 computes the sky from single-scattering atmospheric physics** (Rayleigh + Mie, GPU Gems 2 ch.16 / glsl-atmosphere lineage — 6 view × 3 light samples) for a camera on a ~1 km hill facing north, pitched 22° up, 52° FOV. The sun runs a real arc (east → south-behind-the-viewer → west) sized so it stands 60° off-axis at sunrise/sunset — just outside the ±41° frame, so **the disc is never seen but its forward-scatter glow floods the frame edge**; every hour now differs on its own and mornings ≠ evenings. Layered on the physics: **hourly character curves** (turbidity — clear cool mornings → hazier warm afternoons; cloud coverage — thin night veils → afternoon cumulus → dusk bands; star intensity peaking at midnight; wind), an explicit **blue-hour afterglow** band toward the sun's side (the 6-sample physics under-delivers it), night **airglow ambient + soft moon glow** (moon opposite the sun; never a disc), two-layer perspective clouds lit by the sun (or the moon) with undersides lit at low sun, `1−exp(−x)` tone map. Light theme = sun ≥ +3° with a mild horizon-weighted haze lift for ink contrast; dark = sun ≤ −3°; outside its half each theme holds at the nearest edge. **Perf, measured (GPU-synced via readPixels): 10.8 ms/frame at 2880×1800 (DPR 2) on Simon's M3**, drawn every other rAF (~30fps, 33 ms budget); adaptive DPR drops 2 → 1.25 if the first seconds can't hold ~40fps; paused offscreen/hidden; still frame under reduced motion. Tuning notes: the first pass read as overcast — the turbidity curve was ~2× too hazy and the haze lift too strong; fair-weather clouds need the sky + sunlight filling their shadow side (`shadowCol = ambient·0.85 + lit·0.28·dayness`) or they read as grey slabs.
- **Sky 24h refined against the reference canon (2026-09-05, round 3b):** Simon picked the **24h concept** as the direction; Halo survives as **dark-theme-only** (light version removed at his ask). The shader was rebuilt against studied references (reflect.app live; Firewatch/Daylight canon — dossiers in DESIGN_KNOWLEDGE §6.6): **① a fifth keyframe — BLUE HOUR** (deep indigo + rose afterglow, the previously-missing photographer's hour, reachable in dark mode ~19–20h); **② richer golden hour** (amber horizon ↔ indigo zenith — complementary temperature opposition); **③ a shaped sun corridor** low in the frame (one dominant light event, kept below the text — Reflect's lesson — strongest at the warm hours); **④ two cloud layers** (far bands + near wisps at 2.7× scale/2.4× drift) for parallax depth, stretched into long horizontal bands at low sun, with **underside lighting** when the sun is low (light-offset direction flips with elevation) and a warm additive rim at golden/blue hour; **⑤ living clouds** (the detail field evolves through time, not just translates) + sparse **temperature-tinted stars** + a 6% vibrance lift. New **ink guard**: in the light theme's elevation range the cloud-shadow floor is lifted (`max` toward a light grey) so the dimmed headline span keeps ≥3:1 over thick cloud cores — a little drama traded for guaranteed legibility. Verified: day (light), golden 18:12 (light), blue hour 19:00 (dark), night (dark), halo-light now flat.
- **Hero exploration round 3 — the sky simulator (2026-09-05, same branch):** Simon's verdict on round 2: keep **Halo + Aurora** (Halo the favourite so far), the first shaders are "too simple, not good enough", the light versions "didn't exist" (they did — tuned so quietly they didn't register: **light mode needs ~2-3× dark's alpha to read**, now fixed: aurora light 0.05→0.12, halo light rebuilt as a warm sunlight-on-paper bloom instead of the invisible vignette), and the lead→CTA gap goes **40→32** (24 phones). Dawn preset retired. New centrepiece per his own concept (logged in DESIGN_KNOWLEDGE §6.6): **`SkyField.jsx`** — a procedural sky simulator: eye-level view, sun always behind the viewer (light, never the disc), clouds on a **perspective-projected plane** (compress toward the horizon), palettes driven by **sun elevation** across four keyframes (night/twilight/golden/day) with directional lit rims on the clouds, a two-scale twinkling **star field** at night, dither + site grain against banding. **Theme is the day/night switch** (light = high-key blue day — deep blue only at the very top so black ink keeps AA on the text band; dark = deep night, cloud luminance capped for white ink). Variants: **Sky** (theme-locked noon/midnight) and **Sky 24h** (follows the local clock; elevation **clamped per theme** — light rides sunrise→sunset, dark rides dusk→dawn — so ink never lands on an illegible sky; the switcher grows an **hour scrubber** pill, exploration chrome only). Perf: DPR ≤2, half-rate rAF (~30fps — drift is glacial), synchronous first frame, paused offscreen/hidden, still frame under reduced motion. Switcher: current · refined · aurora · halo · sky · skytime.
- **Hero exploration round 2 — refined layout + three atmospheres (2026-09-05, branch `claude/portfolio-hero-variants-abf8dd`, NOT on main):** Simon's verdict on round 1 (five layout variants): Display "too big", Spread/Poster "not it", the giant Name belongs in the FOOTER not the hero, the Tonal colour device is good *but inside the current 32px view*, and the current hero stays his favourite — optimize it and explore genuinely beautiful **atmosphere** instead of layout. Round 2 therefore: **(a) `HeroRefined`** — the current hero craft-passed, sizes untouched (32/36 · 14/20 · 14 buttons): tonal dim on the connective phrase (secondary ink) inside the headline; spacing regrouped by Gestalt proximity — **20px headline→lead** (one line of the lead's own leading) and **40px lead→CTAs** (2×, the message/action group boundary; was 8/16); the lead gets its own **~62ch reading measure** instead of running the 513-box at ~84 chars/line (the cap bites at wide viewports). **(b) Three WebGL light-fields** behind that same layout (`Atmosphere.jsx`, raw WebGL1, no deps): **aurora** (achromatic domain-warped fbm silk), **dawn** (cool zenith → muted warm horizon + faint stratus; the one preset with restrained chroma — light theme is a pale morning sky), **halo** (single breathing bloom from the top edge — light enters from above). All: low-contrast behind text, in-shader dither (anti-banding), alpha fade over the lower third into the flat ground, full-bleed 100svh reaching up under the glass header (which finally has something to refract), DPR ≤1.5, first frame drawn synchronously, paused when offscreen/hidden, still frame under reduced motion, theme observed live. This nuances the "flat B&W field" decision (§6.8): the canvas stays achromatic — atmosphere is a *localized hero light moment that melts into it*, per Simon's explicit ask. **(c) Footer wordmark** — "Simon Knudsen" at `clamp(48px, 12.2vw, 176px)` (−4.5% tracking, weight 500) between the link columns and the copyright, scroll-revealed; the identity-scale name Simon liked from round 1, in the place he wanted it. Switcher ids: current · refined · aurora · dawn · halo. `WordReveal` learned a `segments` prop (per-run classNames) so the tonal headline keeps the word cascade. COMPANIES data lives in `src/components/home/companies.js`.
- **No card** — left-aligned statement headline set directly over the shader; faded **"Simon Knudsen"** kicker above; **removed** the avatar + "Available for work" pill — *if the type is confident it doesn't need a container.*
- **Headline** custom large scale; `max-width: min(Nch, 100%)` — *never overflows on mobile.*
- **Hero CTAs** — iterated: black/grey → both glass → settled on **primary (solid) "View projects" + glass "Contact"** (primary action solid, secondary glass).
- **Shader rises behind the nav** (negative top margin + `--header-height`) with a subtle **grid overlay** (masked) — atmospheric depth.
- **Height** iterated to taste: 100vh → 76vh → **`calc(64vh + 200px)`**; content vertically centered; `margin-bottom: 0`.

## 3. Home — Worked at (`src/components/home/WorkedAt.*`, `WorkedAtLogos.jsx`)
- **New trust row** (bevel-style "Works with"): **Lenus · Beefit · Zliide · Adtraction** (most-recent first). all four are inline `currentColor` SVGs (theme-aware, muted → brighten on hover). **Beefit** uses the full mark+wordmark lockup (Simon supplied the SVG; recoloured from its near-white fill to currentColor) at `logoScale 1.35` since the lockup is denser/shorter x-height than the others — verified all four share the same vertical centre.
- **Layout: centred flex row** (was a `1fr auto 1fr` 3-col grid tuned to centre Zliide — broke with a 4th company). Now `display:flex; justify-content:center; flex-wrap:wrap` so any company count stays balanced under the label; row stays viewport-centred (verified 720 = 1440/2). Wraps to a centred column ≤600px.
- **Real periods supplied by Simon** (date blocker resolved): Lenus **2025 — Present**, Beefit **2024 — 2025** (acquired by Lenus), Zliide **2023 — 2024**, Adtraction **2021 — 2023** (this is *Adservice*, acquired by Adtraction — Simon chose to keep the surviving "Adtraction" brand + logo + `adtraction.com`). Beefit: Health & fitness, 10+.
- **Removed** the old 4-box stat band.
- **Padding-driven hero + equal gaps**: the hero uses a fixed **128px** padding above (below nav) and below its content (64px on phones) — NOT viewport height — so nothing drifts with screen size. Worked-at sits `margin-top: 0`, `margin-bottom: 128px`, so the gap above (hero's 128 bottom padding) **equals** the gap below (128px). Verified stable across window heights.
- **Hover/focus tooltip on each company** (`.companyChip` + glass-panel `.tooltip`): header is the **brand logo** (not text); shows **Industry · (Period) · Team size** (Role dropped as irrelevant); 8px padding, `radius-xl`, real **glass blur** (24px, now that the reveal `will-change` is released); shows **Industry · Company size · Website** (Website last, a link — Lenus `lenus.io`, Adtraction `adtraction.com`; Zliide none, defunct); 12px padding; **content staggers in** one-by-one (0.45s eased, relaxing); **positioned BELOW the logos** so it never covers the "Worked at" label; **per-logo optical scale** (`--logo-scale`: Lenus 1 / Zliide 0.7 / Adtraction 0.9) so the three read balanced in both the row and the tooltip; keyboard-accessible (`tabindex`/`aria-describedby`/`focus-within`), reduced-motion safe.
- **Tooltip rows → single vertical column** (2026-06): was a two-column `space-between` table (label left, value right). With varying value lengths the ragged gaps read busy; switched to **stacked spec rows** — muted uppercase label (`text-2xs`) above value (`text-sm`), all left-aligned to one axis. Calmer/editorial.
- **Confirmed industries/sizes** (Simon): Lenus *Health & fitness software* 300+ · Beefit *Health & fitness software* (same as Lenus) 10+ · Zliide *Fashion technology* 10+ · Adtraction *Affiliate marketing* 30+.

## 4. Home — Projects (stacking) (`src/components/projects/ProjectGrid.*`)
- **Full-width cards that stack on scroll** (sticky), each a full-bleed image with **content overlaid bottom-left** over a scrim; **removed the "View project" CTA** (whole card is the link).
- **Card height** `clamp(440px, 78vh, 860px)` — *fits the window instead of towering;* image `aspect="fill"` (cover).
- **`.stackLink` fills the card** — *fixed a bug where the bottom-left info anchored mid-card and the image collapsed (the "missing content" on Apple Home / Zliide Dashboard).*
- **Gap 600px** between cards; small per-card peek offset (`--i`).
- **Dim + scale depth cue**: scroll-driven `--dim` darkens AND `--scale` shrinks a card as the next covers it (`scale(1 → 0.9)`, `transform-origin: top center`) so covered cards **recede into the stack** — front card reads largest/closest, cards below get smaller. Both set per frame (rAF), **no CSS transition** (would add jank); disabled under reduced-motion.
- **Shadow removed** — the `--shadow-lg` drop shadow haloed harshly over the photos when cards piled up; depth now comes from scale + dim, not a shadow.
- **Card images are now sharp (no blur).** Removed the blurred-image-copy frost behind the text — only the dark gradient scrim remains for legibility. *(History below kept for reference.)*
- ~~**Frosted text zone via a blurred IMAGE copy (not `backdrop-filter`).**~~ First tried a live `backdrop-filter: blur()` scrim — but blurring 6 full-bleed cards *every scroll frame while they scale* made the stack janky (the "don't animate backdrop-filter" trap). Fix: `.stackBlur` is a **duplicate of the card image with a static `filter: blur(20px)`**, masked to the lower text zone and over-scaled 1.08 so the soft edges stay clipped. A static filter rasterises **once** and is then just composited/scaled by the GPU → no per-frame re-blur → smooth, and the premium frosted look is kept alongside the `scale` depth. Chips/tags use plain translucent bg (no blur). Hidden under `prefers-reduced-transparency`. Plus a dark gradient `.stackScrim` over it for AA legibility.
- **Stack depth scale** reduced to **5%** (1 → 0.95) · flow **gap 160px** (iterated 600 → 300 → 240 → 160, tighter cadence) · stacked **peek 4px** per card (iterated 12 → 10 → 4).
- **Blur as a 0→8px gradient**: `.stackBlur` is `blur(8px)` (toned down from 20) with a linear mask `to top, #000 0%, transparent 58%` so the blur ramps from full at the bottom to sharp by ~58% up — a "blur gradient" from one cached layer (no costly stacked-blur passes).
- **All cards start full available width** (no base scale); depth comes from a **graduated recede** instead: when covered, the first card shrinks most (~−16%) and each later card less (linear `MAX_RECEDE * (1 − i/(n−1))`), so a fully-stacked deck reads ~0.84 → 0.87 → … → 1.0 (front) — first smallest, each progressively larger, same starting size for all.
- **Card header enlarged** to `type-display-sm` (~40px desktop, was `type-heading-sm` 24px) for more presence on the big cards.
- **Section header "Selected projects"** added above the stack (`type-display`, matches the testimonials header pattern).
- *(Reverted)* hover chevron on project **cards** — it belonged on the **nav dropdown rows**, not the cards. Removed from ProjectGrid/ProjectCard; added a right-chevron that fades in on hover at the right edge of each **dropdown `WorkRow`** (`.workChevron`).
- **Card logos use the same optical scale** as the Worked-at row (`COMPANY_SCALE`: Lenus 1 / Zliide 0.7 / Adtraction 0.9) so they're visually balanced.
- **Company logo on each card** (`PROJECT_COMPANY` map → logo, white, **pinned top-left corner**, inset to match the card padding): Zliide projects → Zliide; Adservice-era + campaigns → Adtraction; Apple Home (concept) → none. *Best-effort mapping — confirm.*
- **`variant`**: `stack` (home), `grid` (3-col, used on case-study "More Projects"), `bento` (legacy).

## 5. Home — Testimonials (`src/pages/Home.jsx`, `TestimonialCard.*`)
- **3-column grid** of full recommendations; **logos where available** (Zliide inline SVG, Adservice image on a white chip, Business Academy none).
- **Removed** the big FeaturedQuote and the scrolling logo marquee.

## 6. Footer (`src/components/Footer.*`)
- **Frosted glassmorphism** — footer is transparent + `backdrop-filter: blur(lg) saturate()` + a 1px light-catching top border, so it frosts the **global shader** behind it (no longer its own shader). Solid fallback where backdrop-filter is unsupported.
- **Link hover**: underline grows from the left + slight slide + brighten.

## 6b. Contact page (`src/pages/Contact.*`)
- **New dedicated `/contact` page** (replaces the old footer-scroll). Minimal/editorial per the proven pattern: one confident headline ("Let's make something worth using."), a short human line, and a **frosted-glass card with the email as the big focal action** (`mailto:`, ~40px) + Email/LinkedIn links + the live location/availability chip. Sits over the global shader. Nav "Contact" (desktop + mobile) and the hero "Contact" button now route here; the hover dropdown stays as a quick preview.

## 7. About (`src/pages/About.*`, `PrinciplesList.*`)
- **Gallery animates in on scroll (2026-06-09)** — *the entrance was never seen.* `ImageGrid` tiles are now each a `<Reveal preset="fade-up">` (in-view, **no `Stagger`** so the delays don't compound across many images). Previously `Media` revealed them ~300px early (off-screen), so by the time the gallery scrolled into view the tiles were already shown.
- **Top section** (marckuiper-style): big headline + portrait (memoji placeholder — *awaiting a real B&W photo*) + **floating skill chips** on desktop, **stacked below on mobile**; **grey bg removed**; portrait stage stacks on mobile (*fixed horizontal overflow*).
- **Mission line animates on scroll-into-view** (`WordReveal whenInView`) — *was firing on mount, before the user scrolled to it.*
- **Skills copy restored to Simon's original** (the AI-style rewrite was rejected).
- **Principles** rewritten plainer; **numbered `01 — Section` eyebrows removed** site-wide (read as templated/AI).
- **Awards/Recognition section removed** (not relevant).
- **Pictures lightbox**: portal-rendered, smooth open/close, **prev/next + close (top-right) + "X of Y" counter**, arrow-key nav. Fixed: nav/close buttons now sit **above** the image (z-index — the prev arrow was rendering behind it) and the **prev/next arrows are pinned to the viewport sides (fully outside the image)**; image transition between pictures softened to a clean fade (opacity + subtle scale, no jumpy rise).
- **Principles section ("Principles I design by") removed from About — parked as an idea.** The `<PrinciplesList />` component + 6-principle content are kept in the codebase to re-add later (see Parked ideas below).

## 8. Projects — Case study (`src/pages/ProjectPage.*`)
- **Truly single column** — removed the separate left meta rail entirely (Simon asked repeatedly). The whole case study is one column: the **container fills 100% of the available width**, and readability comes from **capping the text blocks** (lede/prose/steps/textBlock at 60rem) while media/breakouts span full width. Meta (Role · Client · Team · Timeline · Outcome) is a **horizontal header row** above the content; the sticky TOC sidebar is hidden.
- **Removed "Next project"** section.
- **"More Projects" → 2-column grid** (`variant="grid"`, was 3) — bigger cards.
- Breakout media 8px radius; grey image background removed.

## 9. Archive (`src/pages/Archive.*`)
- **2-column larger cards** (was 3); descriptions shown.
- **Tag overflow → "+N" chip + tooltip** (measure-based, one line); all wrappers share one box so the **+N aligns** with the other chips; 1-column on mobile.

## 10. Style guide (`src/pages/StyleGuide.*`)
- Added **status primitive ramps** (success/warning/error/info); **shadows shown on a contrasting stage** so they read; **Radius split into its own section**; **type-micro** added to the type specimen.

## 11. Motion (owned layer — no GSAP/Framer)
- **Page transition**: curtain wipe in the **page background colour** sweeps up to cover, holds, lifts; new page **fades + rises in after** the cover (`.page-enter-nav`, ~480ms delay) — *same-colour curtain = seamless, no contrasting flash.* Skips first load; reduced-motion safe.
- **Theme switch**: View Transitions circular clip-path reveal, slowed to **1200ms** with a gentle ease (relaxed).
- **Intro loader**: "Simon Knudsen" wordmark + a thin line that fills, once per session; reduced-motion skips it.
- **Reduced-motion** safety net throughout.

## 12. Components (cross-cutting)
- **Button**: removed the magnetic-pull effect (*no novelty motion*). Hover = background-colour change (no lift/shadow/sweep) **plus a text-roll**: on hover the label slides up and out while a duplicate rises from below into place (`.roll` overflow-clip + two `.rollText` copies, the 2nd absolutely positioned one line below, both `translateY(-100%)` on hover). Timing matched to the popular GSAP/Framer rolling-text components: **0.55s `cubic-bezier(0.76,0,0.24,1)`** (easeInOutQuart / `power4.inOut`) for a smooth glide. Reduced-motion disables the roll. (Focus-visible ring kept for a11y.)
- **Media**: added **`fill`** aspect mode (cover a flexible parent); **removed the grey placeholder background**; 8px radius; positive preload margin (no dark gaps).
- **ProjectCard / TagRow**: tag-overflow `+N`; image frame 8px. **Phones (≤640px): collapse OFF** — `TagRow` skips the overflow math, shows *every* tag and lets the row flex-wrap onto 2–3 lines (a hover-tooltip `+N` reads poorly on touch). Desktop unchanged (wide stack cards fit all tags on one line anyway; `+N` still guards narrower grid contexts).
- **TestimonialCard**: vertical layout with a logo slot (image or inline component). Home section heading **"What people say" → "Testimonials"**. The **whole quote is now one colour (secondary)** — dropped the brighter first-paragraph "lead line" (`.lead`) per Simon; calmer, less hierarchy-y. **All three cards now carry a logo**: added `BusinessAcademyLogo` (Business Academy Aarhus int'l lockup) to `WorkedAtLogos`, flattened to `currentColor` (its brand teal would be the only cool colour in the warm/neutral system). **Quote-glyph fix**: the decorative `"` was forced back into flow by the `.card > *:not(:first-child){position:relative}` lift rule (specificity 0,2,0 beat `.quoteMark`'s 0,1,0), padding ~105px of dead space onto the card top. Bumped to `.card .quoteMark` so its `position:absolute` wins — content now starts at the 32px padding. *(CSS source-order/specificity trap, CLAUDE.md §7b.)*
- **WorkedAt**: bottom gap now matches the hero's bottom padding per breakpoint (128px desktop, **64px ≤640px**) so the section sits with **equal space above & below at every size** (was 64 above / 128 below on phones). **Phones: 4 logos in a balanced 2×2 grid** (`repeat(2,1fr)`, centred) instead of a tall single column.
- **WordReveal**: `whenInView` prop for scroll-triggered word reveal.
- **ImageGrid**: gallery lightbox with full controls (see About §7).
- **CookieConsent** (`CookieConsent.*` + `lib/consent.js`): on-brand consent card, bottom-left glass-panel, "I use 🍪 cookies" + pill Accept / outline Decline (Neko-friendly style adapted to dark editorial) — *gates Microsoft Clarity (free session recordings + heatmaps + stats); nothing loads until Accept, Decline loads nothing.* Choice persisted in `localStorage`; resumes on return visits; grows from its own corner (`transform-origin: bottom left`); reduced-motion safe; z below the intro loader, above all chrome. *Clarity Project ID `mcb5n1jwt7` is now live in `src/lib/consent.js`* — tracking activates for anyone who accepts. *Why Clarity:* only genuinely-free tool with unlimited visual recordings + stats, and it doesn't sell data (honest with the banner copy).

## 13. Taste & process (`DESIGN_KNOWLEDGE.md`)
- §6 holds the **references**, the **"why it works"** analysis, and the **anti-pattern log** ("what Simon doesn't like").
- **Rule:** when Simon asks to change/redo something, treat it as a signal the original was wrong for his taste — log it as an anti-pattern.
- **§7 "Craft & mastery — the beauty layer" added** (research swarm, 6 expert agents) — the advanced craft layer beyond the §1–4 principles: typography (optical sizing, OpenType, `text-wrap`, font-loading without CLS, variable-font grade), spacing (relational/optical, geometric scale, density tiers), layout (Swiss grids, asymmetric tension, modern CSS — subgrid/container queries/`:has()`/intrinsic grids), motion (spring physics + bounce, interruptibility/velocity, origin-aware, scroll-driven + View Transitions), UX (form mechanics, perceived-perf, microcopy, state design, **portfolio case-study craft**), and UI polish (OKLCH ramps, gradients-as-light, tinted layered shadows, 4-layer glass, squircles, the 1% micro-details, grain). Includes a **§7.7 "invisibles" pre-ship checklist**. *Why:* give the project an expert craft brain, not just correctness — matches Simon's stated premium/editorial taste (§6.7).
- **§6.9 priority hierarchy added** (Simon's own weighting): **typography + spacing/whitespace ≈ 80%** of the beauty → **beautiful imagery/video** → **motion as the calm, supporting ~20%** (relaxing, non-disturbing, felt-not-noticed). Use it to allocate effort and arbitrate trade-offs (higher tier wins; never sacrifice whitespace/type to fit more in).
- **§4.11 motion tooling decision added** (researched 2026, build-vs-buy): **keep the owned CSS+WAAPI+View Transitions layer (~0kb), stay library-free** until a specific gap proves it; if needed, reach for **Motion** (`motion/react`, lazy ~15kb) — *not* GSAP. Notes the 2026 facts: **GSAP now 100% free** (Webflow), **Framer Motion renamed → `motion`**, Motion+ €299 optional. Hybrid rule = add Motion only for exit/unmount choreography, interruptible springs, real drag, or automatic FLIP.

## 14. Parked ideas (removed for now, kept for later)
- **The sky simulator (`components/home/hero/SkyField.jsx`) — parked for iteration, NOT dead (2026-09-05).** Simon's ask: *"save the sky thing for iterations for us."* A physically-based procedural sky (single-scatter Rayleigh + Mie, eye-level on a hill, sun always outside the frame, two-layer perspective clouds, hourly character curves, star field + moon glow, theme = day/night, a 24h clock mode). It stays in the repo and stays runnable — **`npm run dev` → the floating pill (or `?hero=sky` / `?hero=skytime`)** — via `HeroExplorer.jsx`, which is gated behind `import.meta.env.DEV` so none of it reaches production. Measured 10.8 ms/frame at 2880×1800 on an M3; drawn at half rAF (~30 fps) with an adaptive DPR drop. **⚠ KNOWN ISSUE — fix this first next round:** in the **dev server** the sky draws a stale/night frame in light theme at every hour (the hero goes black and the black headline ink disappears into it); the *production* build rendered it correctly all through round 4, so it is a **React StrictMode double-mount** problem, not the shader — `canvas.getContext('webgl')` hands back the *same* context for a canvas React reuses, so the second mount re-initialises over the first one's state. (Forcing `WEBGL_lose_context.loseContext()` in cleanup made it far worse — every compile failed with a null info log — and that call is now removed from both `Atmosphere.jsx` and `SkyField.jsx`. `Atmosphere` survives the double-mount fine; `SkyField` does not.) Likely fix: make setup idempotent per canvas (cache context+program in a `WeakMap`, or delete program/buffer in cleanup) — and verify by scrubbing hours in light theme on `npm run dev`, not only in a preview build. **Open questions for the next round:** is procedural good enough or do we want two AI-generated photographic skies (day/night) with the same fade treatment; whether the light theme can carry a real blue sky without hurting black ink; and the wider *nature* family Simon wants explored (sunlight through leaves, sea haze, rain on glass). Full build notes in §2, concept + reference dossiers in DESIGN_KNOWLEDGE §6.6.
- **`aurora` Atmosphere preset** — the domain-warped light-silk backdrop; still a valid `<Atmosphere preset="aurora" />`, just not what shipped. One click away in the dev explorer.
- **About "Principles I design by"** — a 6-card section (Let the work speak · Clear beats clever · The small stuff matters · Works for everyone · Build it to reuse · Results over screens). Removed from the About page per Simon, but the `<PrinciplesList />` component + content remain in `src/components/about/` to re-add later.
